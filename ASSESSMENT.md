# 现状诊断与可行性评估（为「提升效果 + 商业化」做准备）

> 生成时间：2026-09-12
> 本文件是**第二期**（效果提升 + 前端改善 + 商业化）的事实基础，全部结论有实测取证，见 `PLAN.md` 与各脚本输出。

---

## 一、决定性约束（先说最重要的）

### 🔴 1.1 数据集许可：AICity22 **仅限非商业/学术用途**
实测从 `cityflow/AICity22_Track1_MTMC_Tracking/Dataset License AIC2022.pdf` 提取的原文：

> **2. LIMITATIONS.**
> a. **You may not use the DATASET or any models developed using the DATASET for any commercial or production purpose.**

含义（这是硬约束，不能回避）：
- 当前 68349 条 detection、特征、以及任何**用它训练/衍生的模型**，**都不能用于商业或生产**。
- **这不等于项目没有商业价值** —— 商业价值在**产品/软件层**（检索→确认→回溯管线、UI、部署），这部分是我们自己的代码，许可自定。
- 商业化落地路径 = **在客户自有数据上重建**（客户通常是交警/园区，有自己的摄像头和标注），或使用**商用许可**的训练数据。
- **诚实结论**：当前仓库是「可商业化的软件骨架 + 仅学术/演示可用的数据与模型」。任何对外宣传都不得声称"可直接商用"，否则违反许可。

---

## 二、可用于改造的资源（实测确认）

| 资源 | 状态 |
|---|---|
| 源视频 | ✅ **齐全**：`train/S01`(c001-005)、`train/S03`(c010-015)、`train/S04`(c016-040) 共 **36 个 `vdo.avi`**，覆盖结果集 40 个摄像头 |
| GPU | ✅ `torch 2.9.1+cu128`，`cuda.is_available()==True` |
| 磁盘 | ✅ H 盘剩 **1796 GB** |
| Node/npm | ✅ v24.21.0 / 11.19.0（可建 React/Vue 前端） |
| 网络 | ✅ github.com 可达 |
| 已落盘裁剪图 | ✅ **68349 张已存在**（`output/aicity22_crops/`，`crop_path` 100% 可寻址，全 .jpg）——**无需从视频重抽**。⚠️ 注意别和 `output/cityflow_crops/`（仅 1029 张）搞混，后者是另一份更小的集合 |
| FAISS 索引 | ✅ `clip_vectors.faiss` 68349×512（ViT-B-16，覆盖 100%） |
| 内联 CLIP 向量 | ⚠️ 仅 949 条 ×768（ViT-L-14，1.39%） |
| ReID 特征 | ❌ 全空（`avg_reid_vector` 恒 None） |

---

## 三、效果瓶颈（实测取证，按影响排序）

### 🔴 3.1 属性标注是「系统性相机偏置」，跨镜一致性低于随机
实测（`V*` 车辆 × 摄像头交叉统计）：
| 指标 | 值 |
|---|---|
| 摄像头**内**一致性（主导类型占比均值） | **78.0%** |
| 摄像头**间**一致性（主导类型跨镜相同） | **8.3%**（218 辆车） |
| 随机基线（类型独立随机时的预期一致率） | **31.9%** |

跨镜一致率 **8.3% 远低于随机基线 31.9%** —— 说明同一辆车在不同摄像头里被**系统性标成不同类型**（V0034：c001=面包车、c002=SUV、c003=轿车、c004=面包车、c005=SUV；V0007 的模式与 V0034 **逐摄像头完全相同**）。

**后果**：检索的 `_attribute_filter` 和拼接的 `_score_attribute` 都建立在这套噪声上 → **跨镜属性匹配不仅无用，反而反向有害**（实测弱身份路径正是因此"惩罚正确轨迹、奖励错误车辆"）。

**根因**（据证据推断，未回溯原始管线，不敢下定论）：不是随机噪声（摄像头内 78% 稳定），而是**每摄像头系统性偏置**——很可能是低质量分类器在每路视频上的系统性误判，或标注按摄像头错位。

### 🔴 3.2 两套不兼容的 CLIP 特征空间
512/B-16（检索，覆盖 100%）vs 768/L-14（builder 外观分，覆盖 1.39%）。向量不可混用。已导致一次分析误判（把 `clip_score=0` 误当"无向量"）。详见 `PLAN.md` §0.5。

### 🔴 3.3 无 ReID 特征，跨镜外观全靠 CLIP
`avg_reid_vector` 恒 None。CLIP 是图文对齐模型，**不是为同车跨镜再识别训练的**：实测不同车 cos 0.88–0.90、本车 0.942，**区分度极弱**。

> **补充（调研 agent 实测）**：`src/stitching/scoring.py` 给 reid 分配的权重是 **0.25（车辆）/ 0.35（行人）**（第 120/128 行），且第 280 行有 `avg_reid_vector is not None` 的分支，但该向量恒 None → **这条分支永不触发，25–35% 的拼接权重永久归零**。这就是弱身份路径拼错的**直接根因**。另外 `configs/default.yaml` 里 `weights.vehicle.reid=0.15` 与 scoring.py 硬编码的 0.25 **不一致**（另一处"配置驱动"被破坏）。

### 🔴 3.4 弱身份拼接路径匹配错误
实测 V0034 被拼成 `c004→V0001`、`c002→V0008`。根因 = 3.1 + 3.2 + 3.3 叠加。当前**只可用于强身份（vehicle_id 真值 / 车牌）**。

### 🟠 3.5 前端仍在编造数据
`_convert_trajectory_response` 凭空填置信度、编造进出描述；`map_view.py` 伪造经纬度；`api_backtrack` 只 import 未调用。详见 `PLAN.md` §0.6。

---

## 四、对「效果较好」的可行性判断

**能显著提升、且当前资源足够支撑的**（按收益/成本排序）：
1. **重建属性标注**：从源视频重抽帧，用有预训练权重的**车辆属性识别模型**（颜色/车型）重跑，替换当前系统性偏置的标注。→ 直接修 3.1。
2. **统一 CLIP 特征空间**：用同一模型（ViT-L-14 或 B-16）对 68349 张图重编码，替换两套旧向量。→ 修 3.2。
3. **引入车辆 ReID 特征**：用有预训练权重的车辆 ReID 模型（如 fast-reid / torchreid 的 VehicleID/VeRi 权重）对 68349 张裁剪图重编码，作为跨镜外观分的主信号。→ 修 3.3/3.4。
4. **前端换成现代 Web**（Node 可用）：React/Vue + 高星组件库，替换 Streamlit；同时**删掉编造数据**。
5. **建立可量化的评测**：用 AICity22 的 GT（`train/.../gt` 若有）或按 vehicle_id 构造的真值，对「检索命中率」与「跨镜拼接正确率」出**真实数字**，不再靠感觉。

**成本/风险需注意**：
- 重编码 68349 张图 + 重抽帧是**离线批量任务**，GPU 可用、磁盘够，但耗时长（预计数小时），需后台跑。
- ReID/属性模型的预训练权重下载与**许可**必须逐个核实（VehicleID/VeRi 多为研究用途，商用需另找许可数据）。

---

## 五、下一步

见 `PLAN2.md`（本文件是它的"现状"章节）。技术选型（前端 UI 库、ReID/属性模型）由并行调研 agent 补齐后写入。
