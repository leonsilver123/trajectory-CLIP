# 二期计划：效果提升 + 前端重建 + 商业化（PLAN2）

> 创建：2026-09-12
> 状态图例：`[ ]` 未开始　`[~]` 进行中　`[x]` 完成　`[-]` 放弃
> 事实基础：`ASSESSMENT.md`（全部有实测取证）；技术选型来自两个并行调研 agent（star/license 均为其实际访问 GitHub 核实）

---

## 一、目标（一句话）

把项目从「**能跑但效果差、前端是 Streamlit 演示、数据有系统性缺陷**」提升到「**效果经 GT 指标证明、前端为现代 Web、诚实不编造、具备可商业化软件形态**」，同时**明确标注许可边界，不虚报商用能力**。

三个子目标（按优先级）：
1. **效果**：修掉四大效果瓶颈（属性偏置 / 两套 CLIP / 无 ReID / 弱身份拼错），用 AICity22 的 GT 出**真实指标**。
2. **前端**：用高星组件库重建现代 Web 前端，**删掉所有编造数据**。
3. **商业化**：交付可部署的软件形态 + **诚实**的许可合规说明。

---

## 二、现状与约束（结论版，证据见 ASSESSMENT.md）

**硬约束（必须先接受）**：
- 🔴 **AICity22 数据集及其衍生模型禁止商用/生产**（license 2.a 逐字）。→ 商业价值在**软件层**，数据/模型仅学术/演示。
- ✅ 源视频齐全、GPU 可用、Node 可用、GT 齐全、磁盘 1796GB。

**四个效果瓶颈**：
1. 属性标注是**系统性相机偏置**（跨镜一致率 8.3% < 随机基线 31.9%）
2. 两套 CLIP 空间（512/B-16 vs 768/L-14）
3. **无 ReID 特征**（`avg_reid_vector` 恒 None；`scoring.py` 的 reid 权重 0.25/0.35 因此**死掉**，这是弱身份拼错的直接根因）
4. 弱身份路径拼错目标（前三个叠加）

**关键更正（调研 agent 发现，已复核）**：
- ✅ **68349 张裁剪图其实已经存在**（`output/aicity22_crops/`，100% 覆盖，全 .jpg）——**无需从视频重抽**（主 agent 先前看错目录，以为只有 1029 张）。
- ✅ `track_clip_vectors.faiss` 仅 370 行（17.9%），且 `_load_track_clip_index()` 是死代码。

---

## 三、技术选型

### 3.1 前端（来自 Research-FE，star/license 均实测）
- **框架**：Vite + React 18 + TypeScript（**不用** ant-design-pro 的 Umi，只借鉴其布局，避免框架魔法）
- **组件库**：`antd`（MIT，99.5k★）—— 一个库覆盖 Table(虚拟滚动)/Progress/Tag/Image.PreviewGroup/Timeline/theme.darkAlgorithm
- **图表/甘特**：`echarts`（Apache-2.0，67k★）
- **地图**：`leaflet`（BSD-2，45k★）+ **免 key 的瓦片源**（OSM/CartoDB）作为默认；天地图（CGCS2000，免 GCJ-02 纠偏）作为生产升级并文档说明
- **排除**：高德/百度（商用授权费 5万/年 + 不可去水印）、DHTMLX Gantt（GPL 版本陷阱）、AG Grid（混合许可）、DataV（停更）

### 3.2 后端/算法（来自 Research-BE，star/license 均实测）
| 痛点 | 方案 | 许可 | 现成权重 | 成本 |
|---|---|---|---|---|
| 属性噪声 | **PaddleDetection PP-Vehicle 属性模型**（或 PaddleClas PULC_vehicle_attribute） | Apache-2.0 | ✅ 7.2MB 直链 | 低 |
| 无 ReID | **fast-reid `veri_sbs_R50-ibn.pth`**（VeRi R@1 97.0 / mAP 81.9） | Apache-2.0 | ✅ GitHub Releases 198MB 直链 | 低（约 20s 推理 68349 张） |
| 两套 CLIP | 用 ViT-B-16 重跑那 949 张，统一成 512 维 | — | 已有 | 极低（约 1.2 分钟） |
| 弱身份拼错 | 补 ReID 特征 + 修评分权重 + 时间门控 | — | — | 代码级 |

> **待确认（执行时核实，不阻塞开工）**：VeRi/VehicleID 训练数据本身的许可（关系到"用其权重训出的模型能否商用"——但当前数据本就非商用，此问题仅影响商业化章节的措辞，不影响技术落地）。

---

## 四、任务分解（串并行编排）

### 依赖关系
```
关键路径：T2(ReID) + T1(属性) + T3(CLIP统一) ──▶ T4(重建索引) ──▶ T7(GT评测)
独立可早做：T5(时间对齐)  T6(权重修复)  T8(前端)  T9(前端接真实数据)  T11(文档)
集成：T10(端到端)
```

### 任务清单

| # | 任务 | 内容 | 依赖 |
|---|---|---|---|
| T1 | 重建属性标注 | PP-Vehicle 对 68349 裁剪图重跑，替换噪声标注，落到 datastore | 裁剪图（已有） |
| T2 | 引入 ReID 特征 | fast-reid 对 68349 裁剪图提取，填 `avg_reid_vector`，建 FAISS 索引 | 裁剪图（已有） |
| T3 | 统一 CLIP 空间 | ViT-B-16 重跑 949 张，统一 512 维，重建 clip 索引 | 裁剪图 |
| T4 | 重建索引 + datastore | reid/clip 索引重建 + datastore 更新 | T1,T2,T3 |
| T5 | 时间对齐 | 应用 `cam_timestamp/*.txt` 全局偏移，修 `actual_travel_time` | 无 |
| T6 | 修评分权重 | 修 `scoring.py` 死权重 + config 硬编码不一致 | 无 |
| T7 | GT 评测 | 用 `ground_truth_train/validation.txt` 出检索命中率 + 跨镜匹配 IDF1/mAP | T4,T6 |
| T8 | 新前端 | Vite+React+antd+ECharts+Leaflet，检索/确认/回溯/大屏四页 | 无 |
| T9 | 前端接真实数据 | 删编造数据（confidence=null 显示"未知"等），接 `/trace` 真实字段 | T8 |
| T10 | 端到端打通 | FastAPI 托管前端 dist/，全流程 smoke + 性能 | 全部 |
| T11 | 许可 + 部署文档 | 许可合规说明（AICity22 非商用）+ 部署步骤 | 无 |

### 串并行编排

**波次 1（并行 3 个，文件/资源不冲突）**：
- **Agent A —— T5 + T6**（纯后端代码 + 数据，不碰前端、不抢 GPU 推理）
- **Agent B —— T8（前端脚手架）**：搭 Vite+React+antd 项目，四页骨架 + API 客户端
- **Agent C —— T1（属性重建）**：下载 PP-Vehicle 权重 + 重跑 68349 张（GPU）

**波次 2（等 GPU 空闲，串行做 T2）**：
- **Agent D —— T2（ReID）**：fast-reid 提取特征，填 `avg_reid_vector`（GPU，最关键）
- （T3 很轻，可并入波次 2 由主 agent 或空闲 agent 做）

**波次 3（等数据层完成）**：
- **T4（重建索引）→ T7（GT 评测）**：串行，评测结果写回 PLAN2 + 报告

**波次 4**：
- **T9（前端接真实数据）→ T10（端到端打通）→ T11（文档）**

### 红线（延续一期）
1. **不虚报**：每个效果改进用 GT 指标证明；跑不出改善就如实说。
2. **许可边界**：不声称"可直接商用"；AICity22 数据/模型标注为非商用。
3. **不编造数据**：前端删掉所有凭空填的置信度/描述/经纬度。
4. **不破坏已有可运行路径**：接口向后兼容；`tests/` 全绿（13 failed 基线不变）。
5. **ID 格式不动**：`target_id`/`vehicle_id`/`track_id` 是契约。
6. **串并行不冲突**：GPU 任务不并行（避免 OOM）；前端与后端文件不重叠。

---

## 五、验收标准

- [ ] GT 评测出**真实数字**：检索命中率（按 vehicle_id 真值）、跨镜匹配 IDF1/mAP，且**优于基线**（若没有改善，如实报告并说明原因）
- [ ] `avg_reid_vector` 不再恒 None；弱身份路径对 GT 车辆**拼对**（或如实报告仍未拼对 + 原因）
- [ ] 属性标注跨镜一致率**显著高于 8.3%**（目标 > 60%）
- [ ] 前端为现代 Web，四页（检索/确认/回溯/大屏）端到端可用，**无编造数据**
- [ ] FastAPI 托管前端 dist/，一条命令起全栈
- [ ] 全量测试仍 `13 failed`（基线失败集合不变），新增测试全绿
- [ ] 许可合规说明 + 部署文档齐备

---

## 六、变更记录

- 2026-09-12：创建 PLAN2 + ASSESSMENT.md（现状诊断 + 技术选型）。
- 2026-09-12：**用户已授权全部权限 + 联网**，T1（属性模型）/T2（ReID）解封。**CLIP 定案 = 统一到 ViT-B-16**（检索路径在用、覆盖 100%）。
- 2026-09-12：补充**关键事实**（Research-BE 实测）：① 68349 张裁剪图已全部存在（`output/aicity22_crops/`，100%）→ 无需重抽；② `vehicle_id` 与 GT track_id **同源**（`V0034`↔GT `34`）→ 强身份路径是 GT 真值，**可评测的是弱身份跨镜匹配**；③ `track_clip_vectors.faiss` 仅 370 行（17.9%）且读取端是死代码（双重问题）；④ 零成本修复 E1（zh/en 字段矛盾 90%+）/E2（轨道多数投票）/E3（轨道索引）/E4（时间门控）——其中 E1/E2 会被 T1 属性模型覆盖，E3/E4 仍需做。
- 2026-09-12：**T3 完成**——949 条 768 维重编码为 512 维（`scripts/unify_clip_949.py`，0 失败）。
- 2026-09-12：启动波次 1：T5+T6（P2-Backend）、T8（P2-Frontend）、T2（P2-ReID）。
