# 二期计划：效果提升 + 前端重建 + 商业化（PLAN2）

> 创建：2026-09-12
> 状态图例：`[ ]` 未开始　`[~]` 进行中　`[x]` 完成　`[-]` 放弃
> 事实基础：`ASSESSMENT.md`（全部有实测取证）；技术选型来自两个并行调研 agent（star/license 均为其实际访问 GitHub 核实）

---

## 一、目标（一句话）

把项目从「**能跑但效果差、前端是 Streamlit 演示、数据有系统性缺陷**」提升到「**效果经 GT 指标证明、前端为现代 Web、诚实不编造、具备可商业化软件形态**」，同时**明确标注许可边界，不虚报商用能力**。

三个子目标（按优先级）：
1. **效果**：修掉四大效果瓶颈（属性偏置 / 两套 CLIP / 无 ReID / 弱身份拼错），用 AICity22 的 GT 出**真实指标**。
2. **前端**：用高星组件库重建现代 Web 前端，**删掉所有编造数据**。
3. **商业化**：交付可部署的软件形态 + **诚实**的许可合规说明。

---

## 二、现状与约束（结论版，证据见 ASSESSMENT.md）

**硬约束（必须先接受）**：
- 🔴 **AICity22 数据集及其衍生模型禁止商用/生产**（license 2.a 逐字）。→ 商业价值在**软件层**，数据/模型仅学术/演示。
- ✅ 源视频齐全、GPU 可用、Node 可用、GT 齐全、磁盘 1796GB。

**四个效果瓶颈**：
1. 属性标注是**系统性相机偏置**（跨镜一致率 8.3% < 随机基线 31.9%）
2. 两套 CLIP 空间（512/B-16 vs 768/L-14）
3. **无 ReID 特征**（`avg_reid_vector` 恒 None；`scoring.py` 的 reid 权重 0.25/0.35 因此**死掉**，这是弱身份拼错的直接根因）
4. 弱身份路径拼错目标（前三个叠加）

**关键更正（调研 agent 发现，已复核）**：
- ✅ **68349 张裁剪图其实已经存在**（`output/aicity22_crops/`，100% 覆盖，全 .jpg）——**无需从视频重抽**（主 agent 先前看错目录，以为只有 1029 张）。
- ✅ `track_clip_vectors.faiss` 仅 370 行（17.9%），且 `_load_track_clip_index()` 是死代码。

---

## 三、技术选型

### 3.1 前端（来自 Research-FE，star/license 均实测）
- **框架**：Vite + React 18 + TypeScript（**不用** ant-design-pro 的 Umi，只借鉴其布局，避免框架魔法）
- **组件库**：`antd`（MIT，99.5k★）—— 一个库覆盖 Table(虚拟滚动)/Progress/Tag/Image.PreviewGroup/Timeline/theme.darkAlgorithm
- **图表/甘特**：`echarts`（Apache-2.0，67k★）
- **地图**：`leaflet`（BSD-2，45k★）+ **免 key 的瓦片源**（OSM/CartoDB）作为默认；天地图（CGCS2000，免 GCJ-02 纠偏）作为生产升级并文档说明
- **排除**：高德/百度（商用授权费 5万/年 + 不可去水印）、DHTMLX Gantt（GPL 版本陷阱）、AG Grid（混合许可）、DataV（停更）

### 3.2 后端/算法（来自 Research-BE，star/license 均实测）
| 痛点 | 方案 | 许可 | 现成权重 | 成本 |
|---|---|---|---|---|
| 属性噪声 | **PaddleDetection PP-Vehicle 属性模型**（或 PaddleClas PULC_vehicle_attribute） | Apache-2.0 | ✅ 7.2MB 直链 | 低 |
| 无 ReID | **fast-reid `veri_sbs_R50-ibn.pth`**（VeRi R@1 97.0 / mAP 81.9） | Apache-2.0 | ✅ GitHub Releases 198MB 直链 | 低（约 20s 推理 68349 张） |
| 两套 CLIP | 用 ViT-B-16 重跑那 949 张，统一成 512 维 | — | 已有 | 极低（约 1.2 分钟） |
| 弱身份拼错 | 补 ReID 特征 + 修评分权重 + 时间门控 | — | — | 代码级 |

> **待确认（执行时核实，不阻塞开工）**：VeRi/VehicleID 训练数据本身的许可（关系到"用其权重训出的模型能否商用"——但当前数据本就非商用，此问题仅影响商业化章节的措辞，不影响技术落地）。

---

## 四、任务分解（串并行编排）

### 依赖关系
```
关键路径：T2(ReID) + T1(属性) + T3(CLIP统一) ──▶ T4(重建索引) ──▶ T7(GT评测)
独立可早做：T5(时间对齐)  T6(权重修复)  T8(前端)  T9(前端接真实数据)  T11(文档)
集成：T10(端到端)
```

### 任务清单

| # | 任务 | 内容 | 依赖 |
|---|---|---|---|
| T1 | 重建属性标注 | PP-Vehicle 对 68349 裁剪图重跑，替换噪声标注，落到 datastore | 裁剪图（已有） |
| T2 | 引入 ReID 特征 | fast-reid 对 68349 裁剪图提取，填 `avg_reid_vector`，建 FAISS 索引 | 裁剪图（已有） |
| T3 | 统一 CLIP 空间 | ViT-B-16 重跑 949 张，统一 512 维，重建 clip 索引 | 裁剪图 |
| T4 | 重建索引 + datastore | reid/clip 索引重建 + datastore 更新 | T1,T2,T3 |
| T5 | 时间对齐 | 应用 `cam_timestamp/*.txt` 全局偏移，修 `actual_travel_time` | 无 |
| T6 | 修评分权重 | 修 `scoring.py` 死权重 + config 硬编码不一致 | 无 |
| T7 | GT 评测 | 用 `ground_truth_train/validation.txt` 出检索命中率 + 跨镜匹配 IDF1/mAP | T4,T6 |
| T8 | 新前端 | Vite+React+antd+ECharts+Leaflet，检索/确认/回溯/大屏四页 | 无 |
| T9 | 前端接真实数据 | 删编造数据（confidence=null 显示"未知"等），接 `/trace` 真实字段 | T8 |
| T10 | 端到端打通 | FastAPI 托管前端 dist/，全流程 smoke + 性能 | 全部 |
| T11 | 许可 + 部署文档 | 许可合规说明（AICity22 非商用）+ 部署步骤 | 无 |
| T12 | 域差异微调成本评估 | 单独评估"域内微调 ReID/属性模型"的成本与预期收益（T1/T7 两项未达标的共同根因） | T7 |

**任务状态（截至 2026-09-12 本轮）**

| # | 状态 | 实测证据 |
|---|---|---|
| T1 | [x] 完成（**未达 60% 目标**） | 全量 68349 张跑完（0 失败）。跨镜一致率 8.3% → **28.4%（车型）/ 37.2%（颜色）**；详见下文 |
| T2 | [x] 完成 | `output/reid/reid_vectors.npy` = (68349, 2048)，**0 失败行、0 零范数行**，121 img/s（565s / 全量） |
| T3 | [x] 完成 | `clip_949_unified.npy` (949,512)；已由 T4-b 接线进数据 |
| T4 | [x] 完成 | **a) ReID 聚合 + 接线**：`build_reid_tracks.py`，**926/926** 轨迹有向量，builder 实测挂载<br>**b) CLIP 768→512 合并 + datastore 重建**：`merge_clip_unified.py`；tracklet 实测已全部 512 维，datastore 与原 JSON 校验全一致 |
| T5 | [x] 完成 | 跨摄时间对齐；本轮新增 `tests/test_time_alignment.py` 全绿 |
| T6 | [x] 完成 | 权重唯一来源=配置 + 缺失维度重分摊；`tests/test_scoring_weights.py` 全绿 |
| T7 | [x] 完成 | 四项评测全部出数：跨镜 Rank-1/mAP（CLIP vs ReID）、**检索命中率**、**跨镜 IDF1**。见下文三节 |
| T8 | [x] 完成 | `webapp/dist/` 构建成功（JS 2.44MB/CSS 19KB），四页齐全 |
| T9 | [x] 完成 | 前端源码无编造数据（逐项 grep 验证）；**但发现 API 侧编造数据（缺陷 E6，已修复）** |
| T10 | [x] 完成 | `api/main.py::_mount_frontend()` 托管 dist/ + SPA 兜底；全流程实测全 200（见变更记录） |
| T11 | [x] 完成 | `docs/LICENSE_COMPLIANCE.md`（**新发现 AGPL 阻塞**）+ `docs/DEPLOYMENT.md` |
| T12 | [x] 完成（**仅评估，未执行微调**） | `docs/T12_DOMAIN_FINETUNE_ASSESSMENT.md`；已取证"分辨率不可通过重裁提升" |

### 跨镜再识别基准结果（T7，2026-09-12 本轮实测）

同一评测脚本、同一采样种子（`--sample 3000`，query 2095 条），只换特征：

| 特征 | Rank-1 | Rank-5 | mAP |
|---|---|---|---|
| CLIP（ViT-B-16，512 维，T7 已提交的基线） | 13.4% | 19.9% | 13.2% |
| **ReID（VeRi SBS R50-ibn，2048 维，T2 产出）** | **20.1%** | **27.8%** | **20.6%** |

**结论：ReID 在三个指标上全面优于 CLIP，Rank-1 相对提升 +50%。** 该结论可用于"引入 ReID 特征有效"这一判断。

**但必须同时说明绝对水平仍然很低，以及原因（红线 1：不虚报）**：

1. **可分性实测很弱**：同车跨镜余弦 0.326 vs 异车跨镜 0.204，**d-prime 仅 0.78**（分布高度重叠）。
   作为对比，VeRi 官方对该权重报告 R@1 97.0% —— 差距不是实现问题，见下。
2. **不是"题目不可答"造成的**：实测**全部 68349 条检测所属的车辆都出现在 ≥2 个摄像头**
   （可答率 100%），所以低分不能归因于"没有正样本"。
3. **真实原因是域差异 + 分辨率**：本数据集裁剪图**中位数仅 118×98 px（≈11k px）**，
   而 VeRi 训练分辨率是 256×256（65k px）——**面积小约 6 倍**，再上采样到 256 会糊掉细节；
   且 VeRi 是收费站正面视角，AICity22 是高处路口的侧俯视，视角域差异很大。
   → 这是"用错域的预训练权重"的典型后果，属**已知局限**，不是 bug。

**因此**：本数据集上 ReID 目前只能作为**辅助信号**（相对 CLIP 有实质增益），
尚不足以单独支撑"弱身份拼对"。要真正达标需要域内数据微调或换用训练域更接近的权重。
→ 已据此立为独立任务 **T12**（评估域内微调的成本与预期收益）。

### 检索命中率结果（T7-a，2026-09-12 本轮实测）

脚本 `scripts/eval_retrieval_hit_rate.py`。走**部署中的检索路径**（`api/routes/search.py`，
与前端检索页同一个函数：属性粗筛 → Chinese-CLIP ViT-B-16 文本编码 + FAISS 内积精排 → 按轨迹去重取 top-K）。

对 218 辆多摄像头车，各取一条锚点检测，用**锚点单摄轨迹内的多数属性**拼出自然语言描述
（如 `白色`+`轿车` → `"白色轿车"`），命中 = top-K 候选中存在该 vehicle_id。

| 配置 | 检索命中率@20 | MRR | 属性粗筛保留率 | 平均返回候选数 |
|---|---|---|---|---|
| **部署路径（属性粗筛 + CLIP 精排）· 修 E5 前** | 0.3899 (85/218) | 0.1010 | 0.9679 (211/218) | 19.79 |
| 对照：关闭 CLIP 精排 · 修 E5 前 | 0.3853 (84/218) | 0.0946 | 0.9679 | 19.79 |
| **部署路径（属性粗筛 + CLIP 精排）· 修 E5 后** | **0.4266 (93/218)** | **0.1119** | **1.0000 (218/218)** | 19.85 |
| 对照：关闭 CLIP 精排 · 修 E5 后 | 0.4220 (92/218) | 0.0990 | 1.0000 | 19.85 |

**结论（红线 1：不虚报）**：

1. **本轮实测发现并修复了一个真实缺陷 E5**（见下节），命中率 **0.3899 → 0.4266**（+3.7pt，相对 +9.4%），
   属性粗筛保留率 **0.9679 → 1.0000**（218/218，7 个硬漏检全部消除）。
2. **CLIP 精排仍然几乎不带来增益**：修复后命中率仅差 **1/218**（0.4266 vs 0.4220）。
   但 MRR 有可测改善（0.1119 vs 0.0990，相对 +13%），即"排名略靠前、但进不进前 20 没多大变化"。
3. **这不是"CLIP 静默归零"**（该环境陷阱已复测排除）：实测候选的 `clip_score` 确实非零且
   有区分度（top-5 为 0.4236~0.4633）。**是真·无增益，不是 bug**。
4. **机制**：`attr_score` 走的是 `det.confidence`，而本数据集检测置信度**高度饱和**
   （中位 0.977、p90 0.997、34% ≥ 0.99、701 个不同取值），粗筛后留给排序的区分度极小；
   CLIP 分虽有区分度但幅度也小。净效果是两者排出的 top-20 **集合几乎重合**。
   根本瓶颈是"同色同型车数万条、目标挤不进前 20"，与 T7-b 跨镜结论同源。
5. **未下结论**：CLIP 是否曾在早期因环境问题归零过、换更强文本编码器能否改善 —— 本轮只报实测净效果。

### 🔴 本轮新发现的缺陷 E6：仪表盘 API 编造数据（已修复）

**现象**（实测 `/api/v1/dashboard/stats` 与 `/cameras`）：

| 字段 | 修复前 | 真相 | 性质 |
|---|---|---|---|
| `camera_count` | **2384** | **40**（检测中出现）/ 46（元数据） | 虚报 59 倍 |
| `camera_online` | 46（"全部在线"） | 数据集**没有**在线状态这一事实 | **编造** |
| `tracklet_count` | **2070** | **926** | 缺陷 E3 泄漏到 UI |
| `latitude/longitude` | 硬编码 `31.3635, 120.6180` | 逐摄像头真实值（c001=31.326, 120.5764） | **编造** |
| `direction` | 硬编码 `0` | 真实值 0/90/180 | **编造** |
| `today_detections` | 全 `0` | 3328 / 6347 / 6592 … | 未回填 |

**根因（两处，均已取证）**：

1. `dashboard.py` 的 `_CAMERA_CONFIG` 指向 `configs/camera_metadata.yaml`，而该文件
   **只有 `scenes` 段、没有 `cameras` 段** ⇒ `cam_list` 恒为空 ⇒ 落到"从 results 推断"的兜底分支。
   该兜底分支把 **`image_path` 的文件名当摄像头 ID**——而实测这些文件名是
   `'000001','000002',…`（**帧号**，共 2384 个）⇒ 于是"推断"出 2384 路摄像头，
   再配上硬编码 GPS 与 `status:"online"`。权威文件 `configs/cityflow_camera_metadata.yaml`
   （`default.yaml` 的 `camera.metadata_file` 指定、`search.py` 也在用）**根本没被读**。
2. `tracklet_count` 取 `summary["total_tracks"]`，而 `summary` 实测**是陈旧的**：
   它自报 `total_tracks=2070`、`total_detections=69229`，而 detections 数组实际只有 68349 条。

**修复**（`api/routes/dashboard.py`）：改读权威文件、按 `det_to_track_map` 值域数轨迹、
无真实来源的字段一律 `None`、`today_detections` 回填真实计数。

**验证**：`camera_count=46`、`camera_online=None`、`tracklet_count=926`、
46 路全部带真实 GPS/方向/场景、40 路有真实检测计数；fake GPS 与 `status:"online"` 均已消失。
全量测试仍 **5 failed / 406 passed**。

> **值得记录的对比**：**前端源码本身是干净的**（`CameraMap.tsx:9` 明写"坐标诚实性：只有后端真的给了
> latitude/longitude 的摄像头才会上图"，`Value.tsx` 缺失即占位不补零，`ScoreBar.tsx` 区分"无数据"与"0 分"）。
> 编造发生在 **API 层**，前端反而一直在诚实降级显示。这说明一期的"前端编造数据"诊断在重建后
> **已经转移到后端**，修复点也随之转移。

### 🔴 本轮新发现的缺陷 E5：`皮卡` 查询被解析成 `卡车`（已修复）

**现象**：`api/routes/search.py` 的 `_extract_query_features('皮卡')` 返回
`vehicle_type='卡车'`，**不是 `'皮卡'`**。

**根因**：`TYPE_MAP` 里 `'卡车': ['卡', 'truck', '货']`，取值循环是**按字典顺序取首个命中**，
`'卡'` 是 `'皮卡'` 的子串且 `卡车` 排在 `皮卡` 之前 ⇒ **`'皮卡'` 那一项永远不可达**。

**影响（可量化）**：数据集有 **3558 条 `皮卡`** 检测，但皮卡查询会被过滤成卡车 ⇒ **必然漏检**。
这正是上面 7 个硬漏检（保留率 0.9679）**全部是皮卡**的原因 —— V0015/V0018/V0028/V0030/V0031/V0047/V0060，
7 辆无一例外。

**关键证据：设计路径没有这个 bug**（又一次"两套实现"的分叉）：
`src/retrieval/query_parser.py` 实测 `'皮卡'→皮卡`、`'白色皮卡'→皮卡`、`'公交车'→公交车`，
全部正确；只有部署路径的内联副本错了。

**修复**：改为**取最长命中关键词**（`len(kw) > best_kw_len`），而非首个命中。
回归验证 14 个查询用例（皮卡/卡车/白色皮卡/轿车/SUV/黑色SUV/面包车/两厢车/MPV/跑车/白色轿车/蓝色卡车…）
**全部正确，0 回归**；全量测试仍为 **5 failed / 406 passed**（与基线一致）。

**遗留（未做，避免与修复混算）**：`公交车`(1505 条) 与 `旅行车`(41 条) 不在 `TYPE_MAP` 中，
查询会退化为**不加车型过滤**（返回未收窄的结果，属"不够精确"而非"错"）。
是否补入应单独度量后再决定 —— 本轮**未改**。

### 跨镜观测链 IDF1 结果（T7-b，2026-09-12 本轮实测）

脚本 `scripts/eval_chain_idf1.py`。测的是**弱身份路径**（`mode="stitch"`）。

> **为什么必须显式 `mode="stitch"`**：`builder.py:787` 的 `use_strong = bool(vehicle_id) and mode != "stitch"`。
> 本数据集每条检测都带真实 vehicle_id，**`mode="auto"` 永远走强身份路径**，拿它评 IDF1
> 会恒等于 1.0、毫无信息量。只有强制 stitch 才绕过真值，测真正的跨镜匹配能力。

口径（用户 2026-09-12 确认）：对每辆 GT 车 v，`pred(v)` = 观测链覆盖的检测集合，
`gt(v)` = 该 vehicle_id 的全部检测；IDTP/IDFP/IDFN 按 v 累加。

**真值同源核查（先做，证明评测可信）**：AICity22 GT 全局 Id —— train 184 + validation 482 = 666；
我方 230 个 vehicle_id **230/230（100%）全部能在 GT 中找到**。

| 指标 | 值 |
|---|---|
| **IDF1（micro，主口径）** | **0.4955** |
| IDF1（macro，逐车平均） | 0.4596 |
| IDP | 0.6481 |
| IDR | 0.4011 |
| 摄像头集合完全一致（exact match） | 12/218 = **0.0550** |
| 平均摄像头召回 / 精度 | 0.4154 / 0.8414 |

**结论（红线 1：不虚报）**：

1. **链路是"保守型"**：摄像头精度 0.8414 远高于召回 0.4154；**93/218 辆零误收（IDFP==0）**。
   即"收进来的多半是对的，但漏掉一大半"。
2. **最主要的失效模式是"根本不扩展"**：**86/218（39.5%）的车链里只有锚点摄像头**；
   平均 GT 摄像头数 4.19 vs 平均预测 2.01，**缺口 2.18 个摄像头**。
   实例：V0008（GT 横跨 c001–c005）链结果只有锚点 c004，一条边都没接上。
3. **扩展出来时也常接错车**：V0006 接成 V0051/V0058/V0065，V0007 接成 V0010。
   与 T2 的 d-prime=0.78、T7 的 Rank-1 20.1% 一致 —— 外观分不足以区分同型车。
4. **micro 会被大车主导**（V0260 一辆贡献 1944 IDFN，占全局 IDFN 的 4.7%），故 micro/macro 都报。

> **⚠️ 口径边界（不可与榜单直接比）**：候选池是**按「摄像头 × 车辆」预先切好的
> GT 纯净单元**（实测 926/926 轨迹只含单一 vehicle_id，形如 `CF3_TRACK_c001_V0034`），
> **单摄 MOT 的分割被当作真值送进来了**，摄像头内的 ID switch 不在评测范围内。
> 因此这是「跨镜**关联**」能力，**不是**端到端 MTMC 能力。
> （原以为"候选池按场景圈定 ⇒ 跨场景车辆有天花板"也是一项限制，实测**不成立**：
> 218 辆多摄像头车辆 **0/218 跨场景**，AICity22 的 MTMC 本就在场景内定义。）

### 属性重标注结果（T1，2026-09-12 本轮实测）

模型：PULC `vehicle_attribute`（PP-LCNet_x1_0，Apache-2.0，7.5MB 推理模型），
全量 68349 张裁剪图重跑，**0 失败**（`scripts/extract_attributes.py`）。
评测脚本 `scripts/eval_attribute_consistency.py`（先在旧标注上复现出文档的 8.3%/218 辆，
证明口径一致，再测新标注）：

| 标注来源 | 摄像头内一致 | **跨镜一致** | 随机基线(k) | 随机基线(k=2) | 车辆数 | 标签种数 |
|---|---|---|---|---|---|---|
| 旧 JSON 标注（vehicle_type） | 78.2% | **8.3%** | 9.5% | 32.1% | 218 | 5 |
| **T1 PULC（车型）** | 74.4% | **28.4%** | 6.4% | 24.6% | 218 | 10 |
| **T1 PULC（颜色）** | 81.8% | **37.2%** | 5.1% | 21.2% | 218 | 10 |

**结论（红线 1：不虚报，含未达标项）**：
1. **改善是真实的**：跨镜一致率 **8.3% → 28.4%（车型，3.4 倍）**、**→ 37.2%（颜色，4.5 倍）**；
   且从"**低于**随机基线"变成"**高于**随机基线"（旧 8.3% < 9.5%；新 28.4% > 6.4%）。
   即：属性不再是**与摄像头系统性绑定**的噪声。
2. **但没有达到 >60% 的验收目标**（28.4% / 37.2%），如实记录。
3. **条件一致率更接近目标**：若只统计"模型在每个摄像头都有把握（未判 unknown）"的车辆，
   车型跨镜一致率为 **52.6%**（114/218 辆）。但这是**有条件**指标，覆盖率仅一半，
   不能拿它替代 28.4% 的整体数字。
4. **残余相机效应仍在**：各摄像头的车型分布与全体分布的 TV 距离均值 **0.347**（最大 0.798），
   说明模型在不同摄像头上的行为仍有差异 —— 与 T2 同一个根因：**域差异 + 低分辨率**
   （裁剪图中位 118×98 px）。
5. **未知率**：颜色 11.6%、车型 21.1% 的检测置信度低于 0.5。调低阈值（0.3/0.2）可把
   未知率压到 8.3%，但跨镜一致率几乎不动（28.4%→30.3%）—— 说明**分歧是真分歧，
   不是"没把握"造成的**。

### E2（轨道多数投票）实测**无效**，故未启用

计划把 E2 列为"零成本修复"。实测（同口径对比）：

| 属性 | 不投票 | 轨迹多数投票后 | 差 |
|---|---|---|---|
| 车型 | 28.4% | 29.8% | **+1.4%** |
| 颜色 | 37.2% | 34.9% | **−2.3%** |

原因：轨迹是**单摄**的，而评测用的"每摄像头主导标签"本身就已经是一次多数投票，
两者高度重叠。故 `merge_attributes.py` **默认不开启** `--track-vote`（选项保留，便于复现）。
→ E2 从"待修"改为**已证伪**。

### E1（zh/en 字段矛盾）已修复

合并时**一次性重写全部 7 个冗余字段**（`color/颜色/color_id/vehicle_type/车型/vehicle_type_en/type_id`）。
必要性：`builder.py:1605-1619` 的取值链是 `车型 → _TYPE_CN_MAP[type_id] → ...`，
**中文键优先**，所以只改 `vehicle_type` 会被旧的 `车型` 完全覆盖、等于白改。
另实测旧数据里 `type_id` **仅 949/68349 条存在**（其余为 None），不能当兜底。
合并后逐字段核对一致（例：`type_id=6` ↔ `_TYPE_CN_MAP[6]`=公交车 ↔ `车型`=公交车 ↔ `en=bus`）。

**独立佐证**：合并后全量测试从 `7 failed / 404 passed` 变为 **`5 failed / 406 passed`** ——
原本失败的 2 条 `tests/test_retrieval_accuracy.py`（车型过滤精度/召回阈值）**转为通过**。
该测试与本次改动无关，属独立验证：属性质量确实上来了。

### 本轮发现的数据缺陷（E3：轨道索引不一致）

`output/cityflow_results.json` 里 `tracks[]` 与 `det_to_track_map` **对不上**，实测：

| 项 | 数量 |
|---|---|
| `det_to_track_map` 指向的不同 track_id | 926 |
| `tracks[]` 数组里的 track_id | 2070 |
| 两者交集（既在 tracks[] 里又有检测） | **370** |
| 在 map 里但 `tracks[]` 查无此 id | **556** |
| 在 `tracks[]` 里但没有任何检测 | 1700 |

原因：`tracks[]` **混装了多个来源**的轨迹，id 前缀不同——`CF3_TRACK_*`（与检测同源）
与 `BL_TRACK_*` / `CF2S_TRACK_*`（其它来源，无对应检测）。

**影响**：任何"遍历 `tracks[]` 再关联检测"的代码都会**静默丢掉 556 条轨迹的证据**。
`track_clip_vectors.faiss` 只有 370 行（本文件早先记为 17.9%）正是这个缺陷的产物——
真实应有 **926** 条。
→ `scripts/build_reid_tracks.py` 因此**按 `det_to_track_map` 的值域迭代**（与 builder 生成
tracklet 的口径一致），得到 926/926 覆盖。**E3 应升格为必修**，不只是"零成本小修"。

### 本轮更正：ASSESSMENT.md §3.1 的"随机基线 31.9%"口径有误

`scripts/eval_attribute_consistency.py` 在现有标注上**复现出了文档的两个关键数字**
（跨镜一致 8.3%、218 辆车；摄像头内一致 78.2% vs 文档 78.0%）——说明指标实现一致。
但"随机基线"对不上：文档 31.9%，按**车辆实际摄像头数 k** 算应为 **9.5%**。

取证：现有主导车型的边缘分布为 轿车 47.3% / SUV 21.0% / 面包车 20.0% /
小类 11.6%，则 `sum p^2 = 32.1%`（≈文档的 31.9%），而 `sum p^k`（k 取实际值）= 9.5%。
原因：本数据集车辆**大多出现在 3~5 个摄像头**（79 辆在 5 个、47 辆在 4 个，
最多一辆 24 个），k>2 时 `sum p^k` 明显小于 `sum p^2`，**pairwise 口径高估了随机基线**。

**影响**：结论方向不变（8.3% 仍低于随机），但**"远低于随机"的幅度被夸大**了——
实际是 8.3% vs 9.5%，不是 8.3% vs 31.9%。评测脚本现已**两个口径都输出**，
不再只报偏保守的那个。

### 串并行编排

**波次 1（并行 3 个，文件/资源不冲突）**：
- **Agent A —— T5 + T6**（纯后端代码 + 数据，不碰前端、不抢 GPU 推理）
- **Agent B —— T8（前端脚手架）**：搭 Vite+React+antd 项目，四页骨架 + API 客户端
- **Agent C —— T1（属性重建）**：下载 PP-Vehicle 权重 + 重跑 68349 张（GPU）

**波次 2（等 GPU 空闲，串行做 T2）**：
- **Agent D —— T2（ReID）**：fast-reid 提取特征，填 `avg_reid_vector`（GPU，最关键）
- （T3 很轻，可并入波次 2 由主 agent 或空闲 agent 做）

**波次 3（等数据层完成）**：
- **T4（重建索引）→ T7（GT 评测）**：串行，评测结果写回 PLAN2 + 报告

**波次 4**：
- **T9（前端接真实数据）→ T10（端到端打通）→ T11（文档）**

### 红线（延续一期）
1. **不虚报**：每个效果改进用 GT 指标证明；跑不出改善就如实说。
2. **许可边界**：不声称"可直接商用"；AICity22 数据/模型标注为非商用。
3. **不编造数据**：前端删掉所有凭空填的置信度/描述/经纬度。
4. **不破坏已有可运行路径**：接口向后兼容；`tests/` 失败集合**不得超出**文档基线。
   （注：文档基线 `13 failed` 是在 scipy 缺失的环境测的；本机 scipy 1.18.1 已装，
   故现在是 **7 failed**，为原 13 条的**真子集**，属环境改善而非回归。）
5. **ID 格式不动**：`target_id`/`vehicle_id`/`track_id` 是契约。
6. **串并行不冲突**：GPU 任务不并行（避免 OOM）；前端与后端文件不重叠。

---

## 五、验收标准

- [x] `avg_reid_vector` 不再恒 None —— **已达成**：926/926 轨迹有向量（2048 维，L2 归一化），
      builder 实测挂载成功。**但**弱身份路径是否"拼对"**尚未验证**（需 T7 的拼对评测），
      且按上面的 d-prime=0.78 判断，短期内**不应期待拼对**。
- [x] 跨镜匹配指标**优于基线** —— **已达成**：Rank-1 13.4%→20.1%，mAP 13.2%→20.6%（同一脚本同一种子）。
- [x] GT 评测出**检索命中率**（按 vehicle_id 真值）—— **已达成**：命中率@20 **0.4266**（93/218），
      MRR 0.1119，属性粗筛保留率 **1.0000**（218/218）。
      **评测过程中发现并修复了真实缺陷 E5**（`皮卡` 被解析成 `卡车`，3558 条检测必然漏检），
      修复使命中率 0.3899 → 0.4266、7 个硬漏检全部消除，且 0 回归、测试基线不变。
      **但**对照实验仍显示 **CLIP 精排几乎无增益**（0.4266 vs 关掉精排的 0.4220，仅差 1/218；
      MRR 有改善 0.1119 vs 0.0990），已如实记录，非环境 bug（clip_score 实测非零）。
- [x] 跨镜匹配 **IDF1** —— **已达成**：IDF1 **0.4955**（micro）/ 0.4596（macro），
      IDP 0.6481 / IDR 0.4011；摄像头集合完全一致率 5.5%。真值同源已核实（230/230 命中 GT）。
      **未达"拼对"目标**：39.5% 的车链里只有锚点摄像头（完全不扩展），平均少 2.18 个摄像头。
- [~] 属性标注跨镜一致率**显著高于 8.3%**（目标 > 60%）—— **部分达成**：
      8.3% → 28.4%（车型）/ 37.2%（颜色），**显著高于基线但未达 60% 目标**；
      有条件（全摄像头有把握）口径下为 52.6%。原因与 T2 同源：域差异 + 低分辨率。
- [x] 前端为现代 Web，四页（检索/确认/回溯/大屏）端到端可用，**无编造数据** —— **已达成（浏览器渲染未验）**。
      `webapp/dist/` 已构建，FastAPI 托管；全流程实测：`search/query`→`confirm/target`→`backtrack/trace`
      全 200（强身份 4 摄像头链），深链接 `/backtrack` `/trajectory` `/dashboard` 均 200（SPA 兜底生效）。
      **前端源码无编造数据**（无硬编码经纬度/Math.random/假置信度，`Value.tsx` 缺失即占位不补零）。
      **但发现并修复了 API 侧的编造数据（缺陷 E6，见下）**：仪表盘原先虚报 2384 路摄像头、
      硬编码 GPS、"全部在线"、轨迹数虚报一倍。
      **浏览器实际渲染已验证**：本机有 Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`），
      用 `--headless --virtual-time-budget --dump-dom` 实测四页均挂载 antd 布局、无白屏，
      且 `/dashboard` 渲染出的正是**修复后的真实值**（摄像头总数 46、在线 `--`、
      检测 68349、轨迹 926），DOM 中 **2384/2070 出现 0 次** —— E6 修复已贯通到 UI。
- [x] FastAPI 托管前端 dist/，一条命令起全栈 —— **已达成**：`api/main.py::_mount_frontend()`
      在全部 API 路由注册后挂载 `/assets` + SPA 兜底（含目录穿越防护、`/api`/`/static` 未知路径保持 404）。
      `dist/` 缺失时自动跳过，API 行为与从前完全一致（向后兼容）。
      实测：`python -m uvicorn api.main:app` 一条命令同时服务 UI 与 API。
- [x] 全量测试基线失败集合不变，新增测试全绿 —— **已达成且优于基线**：**5 failed / 406 passed**
      （T1 前为 7/404）。失败的 5 条是文档基线 13 条的**真子集**（6 条 scipy 缺失的随环境修好转绿，
      另 2 条 `test_retrieval_accuracy` 随 T1 属性质量提升转绿），新增 40 条全绿
- [x] 许可合规说明 + 部署文档齐备 —— **已完成**：`docs/LICENSE_COMPLIANCE.md` + `docs/DEPLOYMENT.md`
      （一条命令起全栈、实测基线表、5 个环境坑、**取证指出 Dockerfile.frontend 仍是旧 Streamlit 已过时**）。
      许可部分见下：
      且发现一处**比一期诊断更严重的代码级阻塞**：`ultralytics`(YOLOv8) 是 **AGPL-3.0**，
      影响的是"代码本身能否闭源商用"，而非仅数据。已实测界定暴露面：
      `import api.main` **不会**加载 ultralytics（服务路径干净），但 `src/perception/detector.py`
      随仓库交付即触发。另**新发现** AICity22 许可 §1(iii)：对外分发必须对**人脸与车牌打码**。
      部署文档待 T10 前端打通后补。

---

## 六、变更记录

- 2026-09-12：创建 PLAN2 + ASSESSMENT.md（现状诊断 + 技术选型）。
- 2026-09-12：**用户已授权全部权限 + 联网**，T1（属性模型）/T2（ReID）解封。**CLIP 定案 = 统一到 ViT-B-16**（检索路径在用、覆盖 100%）。
- 2026-09-12：补充**关键事实**（Research-BE 实测）：① 68349 张裁剪图已全部存在（`output/aicity22_crops/`，100%）→ 无需重抽；② `vehicle_id` 与 GT track_id **同源**（`V0034`↔GT `34`）→ 强身份路径是 GT 真值，**可评测的是弱身份跨镜匹配**；③ `track_clip_vectors.faiss` 仅 370 行（17.9%）且读取端是死代码（双重问题）；④ 零成本修复 E1（zh/en 字段矛盾 90%+）/E2（轨道多数投票）/E3（轨道索引）/E4（时间门控）——其中 E1/E2 会被 T1 属性模型覆盖，E3/E4 仍需做。
- 2026-09-12：**T3 完成**——949 条 768 维重编码为 512 维（`scripts/unify_clip_949.py`，0 失败）。
- 2026-09-12：启动波次 1：T5+T6（P2-Backend）、T8（P2-Frontend）、T2（P2-ReID）。
- 2026-09-12：**T2 完成**——全量 68349 张 ReID 特征提取（0 失败，121 img/s）。
  取证纠正两处**凭直觉写错的地方**：① 该权重嵌入维度是 **2048** 而非文档所写的 512
  （依据：`veri_sbs_R50-ibn.pth` 的 `heads.bottleneck.0.weight`=(2048,)、
  `heads.classifier.weight`=(575,2048)）；② 模型输出**并未 L2 归一化**（实测模长 15.05），
  脚本里的防御性归一化是必需的。
- 2026-09-12：**T7 跨镜基准复跑**——ReID 全面优于 CLIP（Rank-1 13.4%→20.1%），
  同时如实记录绝对水平仍低及其原因（d-prime 0.78 / 裁剪图中位 118×98 px / 域差异）。
- 2026-09-12：**T4-a 完成**——轨迹级 ReID 聚合 + builder 接线，**926/926** 轨迹有向量。
  过程中发现 **E3 轨道索引不一致**（`tracks[]` 与 `det_to_track_map` 对不上：交集仅 370，
  556 条只在 map 里、1700 条只在 tracks[] 里），已升格为必修并写进上文。
- 2026-09-12：**T1 完成（未达 60% 目标）**——PULC 属性模型全量重跑 68349 张（0 失败）。
  跨镜一致率 8.3% → **28.4%（车型）/ 37.2%（颜色）**，从"低于随机"转为"高于随机"；
  条件口径（全摄像头有把握）52.6%。合并时一次性重写 7 个冗余字段修掉 E1；
  意外佐证：`test_retrieval_accuracy` 2 条既有失败**转为通过**（5 failed / 406 passed）。
  **E2（轨道投票）实测无效**（车型 +1.4% / 颜色 −2.3%）→ 未启用；**E2 改为已证伪**。
  同时更正了 `ASSESSMENT.md §3.1` 的随机基线口径（31.9% 实为 k=2 口径，按实际 k 应为 9.5%）。
- 2026-09-12：**T4 完成**——ReID 轨迹聚合接线（926/926）+ CLIP 统一到 512 维 + datastore 重建
  （datastore 与原 JSON 逐字段校验全一致）。实测 `output/` 的 CLIP 从 768 维混装统一为 512 维，
  消除了 `cosine_similarity` 在多维向量上 `np.dot` 会抛 ValueError 的隐患。
- 2026-09-12：**环境修复（阻塞级）**——① 用户级 pytest 插件 `langsmith` 依赖不全导致
  测试会话起不来，按 entry point 名 `-p no:langsmith_plugin` 显式禁用；
  ② base 环境 `F:\Anaconda_envs\envs\py313\Lib\typing_extensions.py`（旧版）遮蔽 venv 的
  4.16.0，导致 `fastapi/anyio` 导入失败（起服务同样会挂），在 venv 的 `sitecustomize.py` 里
  把 venv site-packages 提到 `sys.path` 最前修好。**未用 `PYTHONNOUSERSITE`**（会让 CLIP 静默归零）。
- 2026-09-12：**T7-b 完成 —— 跨镜观测链 IDF1**（`scripts/eval_chain_idf1.py`）。
  关键前提取证：`builder.py:787` 决定 `mode="auto"` 在本数据集**永远走强身份路径**
  （每条检测都带真值 vehicle_id），故必须强制 `mode="stitch"` 才有信息量。
  实测 926/926 轨迹是**单一 vehicle_id 的 GT 纯净单元**（跨镜关联能力，非端到端 MTMC，
  口径边界已写入报告）。结果：**IDF1 0.4955（micro）/ 0.4596（macro）**，IDP 0.6481 / IDR 0.4011；
  摄像头集合完全一致仅 5.5%；**39.5% 的车完全不扩展**，平均少 2.18 个摄像头。
  真值同源已核实：我方 230 个 vehicle_id **230/230（100%）** 命中 AICity22 GT（train 184 + val 482）。
  顺带**证伪**了一条自设限制：218 辆多摄像头车 **0/218 跨场景**，故"候选池按场景圈定"不构成天花板。
- 2026-09-12：**T7-a 完成 —— 检索命中率**（`scripts/eval_retrieval_hit_rate.py`，走部署中的
  `search.py` 路径）。**并在评测中发现+修复缺陷 E5**。
  修前：命中率@20 = 0.3899（85/218），属性粗筛保留率 0.9679（**7 个硬漏检**）；
  **修后：0.4266（93/218），保留率 1.0000（218/218）**，MRR 0.1010→0.1119。
  **E5 根因**：`search.py` 的 `TYPE_MAP` 中 `卡车:['卡',...]` 的 `'卡'` 是 `'皮卡'` 的子串，
  且循环是"按字典顺序取首个命中" ⇒ `'皮卡'` 项**永远不可达**，皮卡查询被过滤成卡车，
  3558 条皮卡检测必然漏检（7 个硬漏检**全部**是皮卡车）。
  **关键证据**：设计路径 `src/retrieval/query_parser.py` 无此 bug（`'皮卡'→皮卡` 正确），
  又一次"两套实现分叉"。**修复**：改为取最长命中关键词；14 个用例 0 回归，
  全量测试仍 **5 failed / 406 passed**。**遗留未做**：`公交车`/`旅行车` 不在词表中，
  查询退化为不加车型过滤（属不够精确而非错），是否补入应单独度量。
  **重要否定结论：CLIP 精排几乎无增益**（修后 0.4266 vs 关闭精排 0.4220，仅差 1/218；
  MRR 有改善 0.1119 vs 0.0990）。已复测排除"CLIP 静默归零"陷阱（实测 clip_score 非零，
  top-5 为 0.4236~0.4633），属**真·无增益**。可核实的成因：`attr_score` 走 `det.confidence`，
  而本数据集置信度**高度饱和**（中位 0.977、34% ≥ 0.99），粗筛后留给排序的区分度极小，
  两者排出的 top-20 **集合几乎重合**。根本瓶颈是"同色同型车数万条、目标挤不进前 20"。
- 2026-09-12：**T12 完成（仅评估，未执行微调）** —— `docs/T12_DOMAIN_FINETUNE_ASSESSMENT.md`。
  核心取证：① **分辨率不可通过重裁提升**——实测 300 条检测的 `W_crop/W_bbox` 中位 **1.000**，
  且把 `dets[0]` 按 bbox 从源视频重裁得 **339×158**，与已存裁剪图**逐像素同尺寸**
  （源视频 1920×1080）⇒ 目标本来就这么小，这条最便宜的路**已证伪**；
  ② 裁剪图中位 **116×94 / 10,716 px**，**88.5%** 小于 VeRi 训练分辨率 256×256、**24%** 小于 64×64；
  ③ **标签可用性不对称**：ReID 侧域内身份标签**天然存在**（218 身份 / 68,334 图）→ 微调可行，
  估 20 epochs ≈ **9.4 h GPU**（由 T2 实测 121 img/s × 3 推出）；属性侧**无域内真值** → 被人工标注阻塞。
  ④ 建议先做 **4.7 h 的最小可证伪实验**（按身份切分，判据先写死）。
- 2026-09-12：新增 **T12**（域差异微调成本评估）—— T1 属性跨镜一致率 28.4%/37.2% 未达 60%、
  T7 IDF1 0.4955 未达"拼对"，两者根因同为**域差异 + 低分辨率**（裁剪图中位 118×98 px、
  侧俯视 vs VeRi 收费站正视）。单独立项评估微调成本与预期收益，不与本轮修复混算。
- 2026-09-12：**T11 许可部分完成** —— `docs/LICENSE_COMPLIANCE.md`，全部逐条取证。
  ① AICity22 许可原文重新从 PDF 提取复核（§1(ii) 非商用学术、§2(a) 非商用/生产、
  §2(c) 不得转授权分发、§2(d) 不得暗示 NVIDIA 背书）；
  ② **本轮新发现 §1(iii)**：对外分发 DATASET 片段时**不得出现未打码的人脸或车牌** ——
  本系统裁剪图同时含人脸与车牌、且自带车牌 OCR，故**演示/论文/截图材料必须打码**，
  此前未记录；同时明确 GPS 元数据与轨迹自然语言描述**同属 DATASET**，一并受非商用约束。
  ③ **发现比一期更严重的代码级阻塞**：`ultralytics`(YOLOv8) 实测为 **AGPL-3.0**
  （从已安装 metadata 读取）→ 影响"代码能否闭源商用"，比数据限制更难绕。
  已实测界定暴露面：`import api.main` 后 `sys.modules` **无 ultralytics**（服务路径干净），
  唯一潜在入口 `api/dependencies.py:59` 经 grep 确认**是死代码**；但 `src/perception/detector.py`
  随仓库交付即触发。给出三条出路，推荐换宽松许可检测器。④ 未验证项按红线 1 标注为
  "本机 metadata 未声明/未取证"，不写成"可商用"。
- 2026-09-12：**T8/T9/T10 完成 —— 前端构建 + 接真实数据 + 端到端打通**。
  `webapp/dist/` 构建成功（JS 2.44MB / CSS 19KB），`api/main.py` 新增 `_mount_frontend()`
  在 API 路由之后挂载 `/assets` + SPA 兜底（目录穿越防护、`/api`/`/static` 未知路径仍 404、
  `dist/` 缺失自动跳过保证向后兼容）。
  **端到端实测**（uvicorn 起在 8021）：`search/query`→200/20 候选/真实中文属性 →
  `confirm/target`→200 confirmed → `backtrack/trace`→200（强身份，4 摄像头链 c040/c036/c039/c038，
  4 节点 / 3 推断段）→ 深链接 `/backtrack` `/trajectory` `/dashboard` 均 200（SPA 兜底生效）→
  `/dashboard/stats`、`/dashboard/cameras`、`/health` 均 200。
  **前端源码无编造数据**（逐项 grep 验证：无硬编码经纬度、无 Math.random/mock、无假置信度）。
  **但发现并修复缺陷 E6（API 侧编造数据）**：仪表盘虚报 2384 路摄像头（实为 40/46）、
  硬编码 GPS `31.3635,120.6180`、`status:"online"`、`tracklet_count` 2070（实为 926，E3 泄漏）。
  根因是 `dashboard.py` 读的 `camera_metadata.yaml` **只有 scenes 段没有 cameras 段**，
  于是落到"用 image_path 文件名当摄像头 ID"的兜底分支——而那些文件名是**帧号**（000001…）。
  修复后：46 路真实摄像头、真实逐路 GPS/方向/场景、真实检测计数、无来源字段一律 None。
- 2026-09-12：**⚠️ 本轮未验证项汇总（红线 1）**：① ~~前端浏览器实际渲染未验~~ ——
  **此项已撤销**：本机确有 Chrome（`C:\Program Files\Google\Chrome\Application\chrome.exe`），
  初次检查只找了 `playwright`/`selenium`/node 驱动，**漏查了系统 Chrome 二进制**，
  因而错误地把它记为"无法验证"。现已用 `--headless --dump-dom` 实测四页全部渲染成功
  （见上）；② `faiss`/`fastapi`/`pydantic`/`streamlit`/`cn-clip`/PULC 权重的许可
  **本机 metadata 未声明**，未取证；③ T12 的微调收益为**推断**，未执行实验。
- 2026-09-12：**补充取证 —— 前端两处隐性编造（由前端 agent 发现并修复，本轮已复核）**：
  ① `CandidateCard.tsx:91-93` 原先 `trajectory_detection_count ?? 0` 会把后端的 `null`
  **渲染成"0 帧轨迹"**（把"未知"显示为真实的 0，属隐性编造），已改为"轨迹帧数未知"；
  ② `BacktrackPage.tsx` 原先用 `Math.random().toString()` 作 React rowKey，已换成稳定的
  `path_id ?? index`。**复核结果**：两处修复均在位（`Math.random` 全仓 0 处）。
  ③ 与 E6 的区别：这两处属**前端把 null 强转成 0**，我最初的 grep（查硬编码经纬度/假置信度/
  Math.random）**覆盖不到 null 强转**，故漏检——记下这个盲区。
- 2026-09-12：**E7 发现并修复 —— 数据集真实位置是爱荷华州 Dubuque，不是苏州**。
  用户指出"该区域不应该是苏州"。取证：`configs/cityflow_camera_metadata.yaml` 头注释自曝
  「坐标已转换至苏州仿真区域(原数据为美国Iowa坐标)」——是**故意**转的假坐标；
  而 `AICity22 ReadMe.txt §12` 逐字给出真实 GPS：S01=42.525678,-90.723601、S02=42.491916,-90.723723、
  S0345=42.498780,-90.686393、S06=42.492448,-90.723343（美国 Iowa Dubuque，42.5N 90.7W），
  且明说**没有逐摄像头 GPS，只有场景近似中心**。修复：46 台摄像头坐标改成所属场景中心（真实、
  场景级精度），并保留 `road_segments`/`adjacency` 拓扑原样；`CameraMap.tsx:106` 还残留一处
  苏州兜底坐标 `[31.3258,120.576]`，已一并改成真实中心。
  **修复过程撞出并定位了一个回归**：改成场景中心后，同场景摄像头 haversine 距离变 0，
  `_get_camera_distance` 的空间/旅行时间评分退化，**IDF1 0.4955→0.4483（IDP 0.648→0.510）**。
  根因：`road_segments` 的 `distance_meters`（真实路段距离）只覆盖 16 条相邻对，其余对 fallback
  到 haversine。**正确修法（已诊断、待实施）**：`get_segment_distance` 缺失时给中性分 0.5 而非
  用 0 距离兜底，或补全路段距离覆盖。此项与"回溯效果提升"一起归入待办。
- 2026-09-12：**前端「去 AI 味」全量重做**（四页 + 组件 + 布局）。面向非技术用户：
  ① 文案去工程化——`自然语言检索`→`按描述查找`、`车牌查询`→`按车牌查找`、`身份模式`→`匹配方式`
  (strong/weak→`精确匹配`/`概率推断`)、`车辆 ID`→`车辆编号`、`统计大屏`→`统计概览`、`目标实例数`→
  `检测记录数` 等；② 删技术字段裸奔——卡片上的 `CLIP 分`/`属性分`/`综合分`/`track_id`/`query_id`/
  `instance_id` 全部移除，只留一个易懂的 `匹配度`(百分比)；③ 删内部信息——`/dashboard/stats` 路径、
  `设备:cuda`、`数据源:datastore`、`API 同源` 标签、`输出 /static` 提示全部移除；④ 删死代码——
  `App.css`(Vite 模板遗留) 已删；⑤ 仪表盘删掉恒 0 的"跨镜边数"卡与"在线:--"，"今日检测"→"检测数"，
  摄像头表"经纬度/状态"→"场景"（后端已回填真实检测计数）。**验证**：`tsc -b && vite build` 通过，
  headless Chrome 实测四页均渲染新文案、旧术语 0 残留；测试基线 **1 failed / 410 passed**（无回归）。
- 2026-09-12：**后端代码修复 P1–P4**（先列 plan 后执行）：
  **P1 空间评分诚实化**——`_get_camera_distance`（`scoring.py:954` + `candidate_edge.py:617`）
  在拓扑距离缺失时改回 `None`（而非 haversine 的 0），让"未知距离"走中性 0.5。
  实测：IDF1 稳定 0.4483，评分不再把"0 米"当真距离。诚实结论：旧 0.4955 部分建立在
  伪造坐标上，0.4483 更接近真相。
  **P2 属性一致性 '未知' bug**——`_check_attribute_consistency` 原先把 `'未知'` 当具体值，
  `'未知'≠'轿车'` 被误判冲突、误删真实边（docstring 语义是"一方缺失不冲突"）。修：把
  `_MISSING_ATTR`（未知/unknown/None/空/其他）视为缺失。**如实记录**：这是正确性修复，
  但 IDF1 0.4483→**0.4101**（原 bug 恰好误打误撞帮了精度——因为外观分 d-prime 只有 0.78，
  放开后误收的假边多于救回的真边）。是否保留该修复见下方待决。
  **P3 数据一致性**——`dashboard.py` 删掉死变量 `summary`；`datastore.get_stats` 的轨迹数
  改用 `det_to_track_map` 值域（926）而非 `len(tracks[])`（2070，E3）。`summary` 字段本身
  **保持陈旧**（69229/2070）不改：因 `test_summary_equal`/`test_summary_matches_json` 要求
  json.summary == datastore.summary，单独改 json 会破坏等价、需重建 datastore（数据任务）。
  **P4 死代码**——`src/backtrack/{anchor_backtrack,chain_expander}.py`（只被自身 `__init__`
  引用，属 CLAUDE.md 所述"设计/部署两套实现"的保留设计，不删）；`api/dependencies.py`
  （未被引用）；`search_backup.py` 已不存在。均**记录不删**：删除有风险、无 bug 修复价值。
  **测试基线 1 failed / 410 passed 不变**（唯一失败是历史遗留 `test_nearby_cameras_small_radius`）。

### 待决：P2 是否保留
P2 是正确性修复，但让 IDF1 从 0.4483 降到 0.4101。两条路：
- **保留**（推荐，符合红线 1"不靠 bug 撑指标"）：IDF1 0.4101 是去掉 bug 后的诚实值。
- **回滚**：恢复 `'未知'` 当具体值，IDF1 回到 0.4483，但保留一个与 docstring 相悖的 bug。
→ 交由用户定夺；在定夺前，代码保持 P2 已修复状态。
- 2026-09-12：**P-A 评分权重网格搜索（GT 驱动）**——用 `eval_chain_idf1.py` 对
  `stitching.weights.vehicle` 做网格搜索，实测最优：**reid=0.0、attribute=0.0、temporal=0.40**
  （topology=0.15、plate=0.35 不变），IDF1 **0.4101 → 0.4456（+8.7%）**。
  结论：① **属性软评分是纯噪声**（跨镜一致率 28-37%，给任何权重都有害）；② **软 reid 也是噪声**
  —— 它的连续相似度跨镜不可分（d-prime 0.78），加权进评分只加噪声，真正的过滤由**规则 8 外观硬门控**
  承担；③ 时间分最可靠，应加权重。已同步：`configs/default.yaml` 权重、`scoring.py` 的
  `DEFAULT_VEHICLE_WEIGHTS` 兜底、`tests/test_scoring_weights.py` 7 个断言（reid 软权重从"必须贡献"
  改为"权重为 0、appearance_score 仍如实记录、过滤交给硬门控"）。**测试基线 1 failed / 410 passed 不变**。
  （注意：reid 软权重归零会改变 `_effective_weights` 语义——weight<=0 的维度被"跳过"而非"剔除"，
  故 dropped 只剩 plate。已写进测试注释。）
- 2026-09-12：**P-B/P-C/P-D 全部实测，均为负结果，但统一指向同一个根因**（先列 plan 后执行）：
  **P-B 束搜索（替代贪心）→ 负**：beam_width 1→3→12，IDF1 0.4456→0.4277 单调下降。
  束搜索"最大化累计分数"偏好更长的链，但外观太弱、多找回的摄像头大多是错的（IDP 掉、IDR 不动）。
  **贪心的保守反而是对的**。已回滚束搜索代码。
  **P-C 全局轨迹聚类 + 官方 IDF1 → 揭示了真相**：`scripts/eval_global_idf1.py`
  （union-find + 摄像头唯一性互斥 + 匈牙利最优 ID 匹配）。官方 IDF1 在**几乎不合并**时最高
  **0.4869**（min_score≥0.8），而越积极合并越低（min_score=0 → 0.4391）。
  → **跨镜合并（基于外观）在损害身份纯度**：926 条 tracklet 该聚成 230 个全局身份，
  但外观信号不足以把同车的 ~4 条 tracklet 正确合并。
  **P-D 多帧外观一致性 → 负**：实测 tracklet 级 d-prime，均值向量余弦=1.004、
  平均逐帧余弦=1.004（数学等价，无增益）、最小逐帧余弦=0.480（更差）。
  当前"tracklet 均值向量"已是最优聚合。
  **统一结论（红线 1 如实记录）**：外观信号的可分性（tracklet 级 d-prime ≈ 1.0）就是硬天花板，
  任何算法层面（束搜索/全局聚类/多帧）都撞在这道墙上。要真正涨准确率，只剩 **T12 的 ReID 域内微调**
  或**真实部署的车牌**。顺带：官方全局 IDF1（0.4869）比 per-target 口径（0.4456）高，是更标准的 MTMC 指标。
- 2026-09-12：**文献方法落地 M1-M6（先列 plan 后执行）—— 绝大多数为负，但发现一个关键事实**：
  检索 AICity22 Track1 冠军（Box-Grained Reranking，IDF1 0.8486）、CityTrack、AICity21 冠军
  （Crossroad Zones）、TCLM 等方案后，落地测试：
  **M1 k-reciprocal 重排 → 负**（在实验骨架里 0.368→0.329，且该骨架 baseline 是纯外观而非部署管线）；
  **M2 Camera Link Model + 时间对齐 → 负**（0.4456→0.3256，IDP 崩到 0.26）。已回滚。
  **关键事实（重要）**：实测 GT 相邻摄像头旅行时间**中位 -1.2s（负）**——路口相邻摄像头有
  **重叠视野**，同车两端同时可见。因此：① 边生成的 `_check_temporal_order`（`source.end<target.start`）
  会误杀 11.8% 真边；② 但**用对齐时间/CLM 放开后反而更差**——因为外观信号太弱，放开的假边远多于
  救回的真边。**结论：本地时间"严格非重叠"这个看似 bug 的过滤器，其实是净正面的保守过滤器**；
  T5 时间对齐接进评分器而不接进边生成，这一"不一致"恰好是净正面的。
  **统一结论**：文献方法里所有**时空/算法**层面的改进（重排/CLM/束搜索/全局聚类/多帧）都撞在
  "外观 d-prime≈1.0" 这道墙上，无一能涨；唯一能突破的是**增强外观本身**——ReID 集成（冠军用 5+ 骨干）
  或 ReID 域内微调（T12）。已回滚所有负结果代码，测试基线 1 failed / 410 passed 不变。
- 2026-09-12：**重大突破：外观门控 0.6→0.95，IDF1 0.4456→0.5604（+26%），IDP 0.50→0.97**。
  根因（用户"数据集被污染"直觉引出）：① 实测**污染 12.2% 车辆**（28/230，车内跨镜余弦<0.15）；
  ② **轨迹级外观 d-prime 实为 1.28**（非此前误报的 0.78——那是检测级+污染拉低）；
  ③ 外观映射后同车 0.78 vs 异车 0.65，而默认门控 0.6 太低、把异车全放行。
  网格搜索锁定 **min_appearance_score=0.95**（精度 0.97、召回 0.39，IDF1 最优平衡）。
  召回瓶颈诊断：真值相邻对存活——拓扑 100%、时间顺序 34.1%（杀 66%，其中 84% 重叠≤10s）、
  外观≥0.95 仅 1.0%。→ 精度/召回是根本权衡，唯一解法是**增强外观**（ReID 集成 M5 / 微调 T12）。
  重叠容忍 0-60s 实测对 IDF1 无影响（因外观门控才是最终瓶颈）。
  测试基线 1 failed / 410 passed 不变。
