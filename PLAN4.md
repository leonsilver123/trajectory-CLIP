# 四期计划：轨迹还原动画 + 简历声明的实现核对（PLAN4）

> 创建：2026-09-21
> 状态图例：`[ ]` 未开始　`[~]` 进行中　`[x]` 完成　`[-]` 放弃　`[?]` 待定
> **执行前先读第二节的设计约束** —— 其中一条（摄像头坐标不可用）直接否决了"地图动画"方案。
> 前序：`PLAN.md` / `PLAN2.md` / `PLAN3.md`

---

## 一、先回答「还有什么没实现」

对照你简历的四条声明，逐条核对**本仓库**的实现情况。

### 1.1 已经实现且可验证的

| 简历声明 | 本仓库状态 |
|---|---|
| 跨镜检索与轨迹回溯平台、离散观测链（观测段/推断段）+ 置信度 | ✅ 完整实现。`observation_segments` / `inference_segments` 是两种数据结构，每段带 `basis` 依据标注 |
| 覆盖目标检索 / 候选比对 / 人工确认 / 轨迹回溯 | ✅ 四步闭环，由后端会话状态机强制（非法状态转移返回 409） |
| YOLOv8x + ByteTrack 生成 Tracklet | ✅ `src/perception/`，`scripts/preprocess_video.py` 可端到端跑 |
| Chinese-CLIP 双塔图文对齐 | ✅ ViT-B-16 / 512 维 + FAISS IndexFlatIP（68,349 条） |
| 属性一致性约束重排（颜色/车型/方向） | ✅ 在线检索的第二阶段 |
| 轨迹级多帧聚合 | ✅ `scripts/build_reid_tracks.py` → `track_reid_vectors.npy`（926 条 × 2048 维） |
| 六维跨镜评分函数 | ✅ 代码完整，六维俱全 |

### 1.2 本仓库没有、但你说业务系统有的（保持现状，不动）

| 简历声明 | 本仓库 | 说明 |
|---|---|---|
| OSNet 提 ReID | 用 fast-reid SBS R50-ibn | 设计口径是 OSNet（`feature_extractor.py` 的默认参数仍是 `osnet_x1_0`） |
| BLIP 交叉注意力精排 | 代码中零出现 | 且有一条测试禁止前端出现 BLIP 标识 |
| Hard Negative Mining / Batch Hard Triplet / BCE+Ranking | 无训练代码 | 本仓库全部使用预训练权重 |
| P@10 0.850→0.973、F1 0.819 | 论文口径 | 本仓库实测是命中率@20 0.4266、IDF1 0.56（另一数据集） |

**处置：不动。** 这些是业务系统的能力，你已确认属实；面试材料第八节已写明两套系统的边界说法。

### 1.3 真正没做到的（本计划要做的）

| # | 缺口 | 性质 |
|---|---|---|
| **1** | **轨迹还原只是一个静态甘特图**，不是你要的"类似视频可以动的"回放 | 🔴 **核心缺口，本计划主体** |
| 2 | 六维评分在本数据集上**实际只有两维生效**（零车牌覆盖 → plate 恒被剔除） | 🟡 已查明并记录，属数据限制非实现缺陷 |
| 3 | 跨镜拼接精度不足（IDF1 0.41–0.56） | 🟡 研究任务，PLAN3 已排除 |
| 4 | 检索命中率@20 仅 0.4266 | 🟡 同上 |

**结论**：除"轨迹还原动画"外，简历声明在本仓库范围内**没有其他未实现项**；
其余差异要么属于业务系统（你已确认有），要么属于数据/精度限制（已取证并记录）。

---

## 二、轨迹还原动画：设计

### 2.1 先说一个否决性约束（为什么不做地图动画）

**46 个摄像头的 GPS 坐标只有 4 组不同值** —— 每个场景内所有摄像头共用同一个坐标（场景中心近似值）：

```
(42.525678, -90.723601) -> c001 c002 c003 c004 c005
(42.491916, -90.723723) -> c006 c007 c008 c009
(42.498780, -90.686393) -> c010 ... c040   (31 个)
(42.492448, -90.723343) -> c041 c042 c043 c044 c045 c046
```

取证：`configs/cityflow_camera_metadata.yaml` + 上述分组统计。

→ **地图上的车会在 4 个点之间瞬移，场景内毫无运动**。任何基于 GPS 的动画都是假的。
（这也解释了为什么 `scoring.py` 里"距离为 0 视为未知"——同场景摄像头算出的 haversine 距离就是 0。）

**因此动画必须是非地理方案。**

### 2.2 方案：三栏联动的「证据回放」

核心思想：**让真实数据动起来，而不是让车"看起来在动"**。

```
┌─────────────────────────────────────────────────────────────┐
│  播放控制：▶/⏸  速度 1x/4x/16x   时间游标 ▓▓▓▓░░░░░░        │
├──────────────────┬──────────────────────┬───────────────────┤
│ ① 摄像头泳道      │ ② 当前视野内的真实运动 │ ③ 参数面板         │
│                  │                      │                   │
│ c002 ▓▓▓▓░░░░    │  ┌────────────────┐  │ 全局时间 00:01:38  │
│       ╲ 推断      │  │   摄像头画面    │  │ 本机时间 00:00:12  │
│ c004      ▓▓▓▓▓▓  │  │      ▪←车      │  │ 摄像头   c004      │
│            ╲ 推断 │  │   ╱            │  │ 方向     由西向东   │
│ c005         ▓▓▓▓ │  │  ╱ 尾迹(真实)  │  │ 置信度   0.86      │
│                  │  └────────────────┘  │ 段内速度 ~32 km/h  │
│ 实心=观测 虚线=推断│  [真实裁剪图]        │ 依据     观测段(实拍)│
└──────────────────┴──────────────────────┴───────────────────┘
```

**① 摄像头泳道 + 时间游标**
- 每个经过的摄像头一条泳道，横轴为全局时间
- 观测段画实心条；摄像头之间画虚线连接（推断段）
- 游标随播放匀速右移；跨摄像头时游标**沿虚线滑动**，该区间整体标注"推断中"

**② 当前摄像头视野内的真实运动**（本方案的核心价值）
- 一个矩形代表摄像头画面（按真实宽高比）
- 车辆用一个简单矩形图标表示，其位置来自该帧**真实 bbox 中心**（归一化到画面）
- 尾迹 = 同一摄像头内已走过的 bbox 中心连线 —— **这是真实数据，不是插值**
- 旁边并排显示该帧的真实裁剪图（`crop_path`），做到"动起来的是实拍画面"

**③ 参数面板**（随播放实时变化）
- 全局时间 / 本机时间（两者都在数据里，且必须并排显示——跨镜时间对齐是本项目的关键设计）
- 摄像头 ID / 名称、方向、当前帧检测置信度
- 段内速度：由相邻帧 bbox 位移 ÷ 时间差估算，**必须标注"估算"**
- 依据：观测段（实拍）还是推断段（概率）

### 2.3 三条诚实红线（本项目的既有原则，动画必须遵守）

1. **跨摄像头时不做平滑插值**。那段路没有摄像头拍到，平滑移动等于编造。
   游标沿虚线走，画面区显示明确的"推断段 · 非实拍"占位，不显示任何伪造的车辆位置。
2. **速度是估算的**。用 bbox 像素位移换算速度依赖相机标定，我们没有；只能给"像素位移/秒"或按段平均的粗估，且必须标注。
3. **没有数据就不显示**。缺失的帧、缺失的 bbox、缺失的方向一律显示 `—`，不回填。

### 2.4 需要后端补充的字段（小改动，向后兼容）

`src/trajectory/builder.py::build_camera_timeline()` 的 `frames[]` 目前是：

```python
{"frame_id":…, "timestamp":…, "crop_path":…, "confidence":…}
```

底层 `cam_dets` **本来就有 bbox**，只是没往上传。需新增：

| 新字段 | 来源 | 用途 |
|---|---|---|
| `bbox` | `det["bbox"]` | 画面内位置 |
| `frame_size` | 待定（见下） | 归一化分母 |
| `global_timestamp` | `to_global_time()` | 泳道横轴用全局时间 |

**`frame_size` 怎么来**：检测的 bbox 是原始视频像素坐标，归一化需要画面宽高。
三个候选来源（按可靠性排序）：
1. 从视频元数据读（`cv2.VideoCapture` 的 `CAP_PROP_FRAME_WIDTH/HEIGHT`）—— 需读 40+ 个视频，慢但准
2. 从 `cam_framenum/` 或数据集 ReadMe 取 —— 待查
3. 用该摄像头所有 bbox 的最大值推断 —— 快但只是下界

→ **本计划采用 3 作默认（NaN 安全），并在字段里标明来源**；若日后要精确，再走 1 做离线落盘。

### 2.5 前端实现位置

- 新组件 `webapp/src/components/TrajectoryPlayer.tsx`（三栏 + 播放控制）
- 复用现有的 `BasisTag.tsx`（依据标注）、`Value.tsx`（缺失值统一显示 `—`）
- 接入 `webapp/src/pages/TrajectoryPage.tsx`，与现有 `TimelineGantt` 并存（甘特看全局、回放看细节）
- 纯前端状态驱动，不引入新依赖（不用动画库；`requestAnimationFrame` 足够）

---

## 三、任务清单

### 阶段 1：后端补字段（小、可验证）

- `[ ]` **T1** `build_camera_timeline()` 的 `frames[]` 增加 `bbox`、`bbox_norm`、`global_timestamp`
  - **验收**：`POST /api/v1/backtrack/trajectory` 返回的每条 frame 含 `bbox`；对无 bbox 的检测返回 `null` 而非 0
  - **回归**：现有 `tests/test_trajectory_builder.py` 全绿（新增字段是追加，不删旧字段）
- `[ ]` **T2** 为归一化提供 `frame_size`，来源写明
  - **验收**：字段里有 `bbox_norm_source` 说明来自哪；无法获取时 `bbox_norm` 为 `null`
- `[ ]` **T3** 新增测试 `tests/test_timeline_frames.py`
  - 断言：frames 按时间升序、bbox 为 4 元组或 null、归一化值落在 [0,1] 或 null

### 阶段 2：前端播放器

- `[ ]` **T4** `TrajectoryPlayer.tsx` 骨架：三栏布局 + 播放/暂停 + 时间游标
  - **验收**：浏览器里游标能匀速走过整条时间线，暂停后停住
- `[ ]` **T5** 泳道视图：观测段实心、推断段虚线、游标跨镜时显示"推断中"
  - **验收**：肉眼确认虚线段没有任何车辆位置渲染
- `[ ]` **T6** 视野视图：真实 bbox → 画面内位置 + 尾迹 + 并排真实裁剪图
  - **验收**：同一摄像头内车辆图标随帧移动；尾迹为直线段连成的折线（真实数据）
- `[ ]` **T7** 参数面板：全局/本机时间、摄像头、方向、置信度、估算速度、依据
  - **验收**：缺失字段显示 `—`；速度旁有"估算"标注
- `[ ]` **T8** 速度控制 1x/4x/16x + 按摄像头跳转
- `[ ]` **T9** 接入 `TrajectoryPage.tsx`

### 阶段 3：验收

- `[ ]` **T10** 用真实 query_id 跑一遍完整回放（截图 + 人工确认）
- `[ ]` **T11** 前端不编造数据的守护测试扩展到新组件（`test_frontend_no_fabrication.py` 已扫 `webapp/src`，会自动覆盖）
- `[ ]` **T12** 更新 `CLAUDE.md` 与 `docs/USER_GUIDE.md`

### 阶段 4：提交

- `[ ]` **T13** PLAN3 的清理成果 + 本计划的成果一并 commit & push 到 GitHub

---

## 四、开工前需要确认的两个取舍

1. **画面宽高比**：拿不到真实画面尺寸时，是用固定 16:9 占位（并在 UI 上标注"比例未知"），还是干脆不显示视野矩形、只显示裁剪图？
   → **默认取前者**：固定比例 + 显式标注，信息量更大且不撒谎。
2. **速度显示**：没有相机标定，"km/h"是编的。
   → **默认只显示"像素/秒"并标注**，不给 km/h。

---

## 五、变更记录

| 日期 | 条目 | 改动 | 验收结果 |
|---|---|---|---|
| 2026-09-21 | — | 计划创建 | 未动代码 |
| 2026-09-21 | **T1** | `src/trajectory/builder.py`：新增 `_timeline_frame()` 与 `_camera_frame_size()`，`frames[]` 追加 `bbox` / `bbox_norm` / `global_timestamp` / `frame_size` / `frame_size_source`（**纯追加，旧字段一个未动**） | 真实数据：4 摄像头 / 213 帧，**213/213 都有 `bbox_norm`**，无越界值；`frame_size=[1600,1200]` 由检测框范围推断 |
| 2026-09-21 | **T2** | 画面尺寸来源标注为 `inferred_from_detections`（**下界估计**，非实测），拿不到时为 `unavailable` + 各字段 `null` | 前端据此显示"画面比例未知"提示 |
| 2026-09-21 | **T3** | 新增 `tests/test_timeline_frames.py`（13 项） | 与 `test_trajectory_builder.py` 合并跑 **36 passed**，无回归 |
| 2026-09-21 | **T4–T9** | 新增 `webapp/src/components/TrajectoryPlayer.tsx`（三栏联动 + 播放控制 + 速度 1x/4x/16x + 上一段/下一段），扩展 `TrackFrame` 类型，接入 `TrajectoryPage` | `npm run typecheck` 通过；`npm run build` 成功（2.47MB） |
| 2026-09-21 | 顺带修 | `TrajectoryPage` 的**深链接不生效**：URL 带 `instance_id` 时只预填输入框、不触发查询。加了 mount 时自动查询（`useRef` 守卫防死循环） | 此前无头浏览器截图停在空表单；修复后直接出结果 |
| 2026-09-21 | **T10** | 无头 Chrome 真实浏览器验收（截图见下） | 三栏全部正确渲染；**动画确实推进**：按钮转「暂停」、游标右移、置信度 `1.000→0.997`、全局时间 `00:02:55→00:02:56` —— 帧确实在变 |
| 2026-09-21 | **T11** | 前端守护测试自动覆盖新组件（`test_frontend_no_fabrication.py` 扫描 `webapp/src`） | `39 passed`（含 timeline / static / 前端守护三组） |
| 2026-09-21 | **T12** | 更新 `CLAUDE.md` 与 PLAN4 本记录 | — |

### T10 验收要点（无头浏览器实测）

```
http://127.0.0.1:8021/trajectory?instance_id=BL_c039_V0312_000028
```

- **① 泳道**：c040 / c036 / c039 / c038 四条；实心蓝=观测段、虚线框=推断段；红色游标随播放移动
- **② 画面内位置**：红色矩形来自该帧**真实 bbox**（`cx=0.91` → 渲染在画面右侧，与数据一致）；
  下方并排显示**该帧实拍裁剪图**（白色轿车）
- **③ 参数**：全局时间 `2020-01-01 00:02:55` / 本机时间 `2020-01-01 00:00:00`（两者并排，
  正是本项目跨摄时间对齐的关键设计）、方向「由南向北」、置信度、段内速度
  `0.0643 画面比例/秒（估算）`

**未验证的部分（如实记录）**：无头环境下 `requestAnimationFrame` 只推进了约 1 秒
（虚拟时间不按 60fps 驱动 rAF），因此**逐帧动画的流畅度没有实测**；
已验证的是"游标会推进、帧会切换、面板随帧变化"这一机制本身。
