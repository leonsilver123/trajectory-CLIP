# 交通风险感知子系统｜文本驱动的交通目标检索与跨镜时空回溯

## 一、项目完整描述

本项目面向苏州市相城区道路交通风险感知与事故回溯场景，构建多摄像头视频流下的人车目标检索与时空回溯算法链路。针对传统文搜图只能返回相似图片、难以支撑目标定位和轨迹排查的问题，系统将多路道路监控视频预先结构化为目标实例库、单摄轨迹库和跨摄像头轨迹候选图。用户输入"蓝色背包的男人""黑色轿车""某车牌号车辆"等目标描述后，系统先通过文本解析、属性过滤和图文向量检索返回候选目标图片；用户确认目标后，系统以该目标实例为锚点，在跨镜轨迹候选图中回溯其经过的摄像头序列、关键时间节点、观测证据和可能路径，实现从"图片级检索"到"目标级时空回溯"的能力升级。

考虑到道路摄像头主要分布在路口、卡口和重点路段，覆盖范围并不连续，系统不将结果表述为连续真实轨迹，而是将摄像头视野内检测到的单摄轨迹作为真实观测段，将摄像头之间的连接关系作为带置信度的推断段，融合目标外观相似度、属性一致性、车牌识别结果、摄像头拓扑、道路方向和合理旅行时间约束，输出目标的高置信时空经过链、候选路网路径和关联置信度，为交通事故回溯、风险目标定位、重点车辆排查和后续人工复核提供支撑。

---

## 二、一句话项目定位

> **基于多摄像头视频结构化、跨镜轨迹拼接和图文检索的交通目标时空回溯系统。**

适合简历的表述：

> **设计"目标实例召回＋用户确认＋跨镜时空回溯"的交通视频检索算法链路，在道路摄像头覆盖不连续条件下，返回目标经过摄像头序列、关键时间节点、候选路径和轨迹置信度。**

---

## 三、整体技术路径

整体路径为"后台预结构化 → 前台秒级检索 → 用户确认 → 轨迹回溯"：

```text
多摄像头视频流
  ↓
视频接入与摄像头元数据治理
  ↓
目标检测、属性识别、车牌 OCR、单摄跟踪
  ↓
生成目标实例库和单摄 tracklet
  ↓
基于时空可达性和外观一致性构建跨镜轨迹候选图
  ↓
用户输入目标描述
  ↓
文本解析、结构化过滤、图文向量召回
  ↓
返回候选目标图片
  ↓
用户确认目标
  ↓
以确认目标为锚点回溯跨镜观测链
  ↓
输出观测段、推断段、候选路径、时间节点和置信度
```

---

## 四、核心算法架构

### 4.1 视频接入与数据治理层

解决"视频能不能用"的问题。需要整理的数据包括：

| 数据       | 用途               |
| ---------- | ------------------ |
| 摄像头编号 | 绑定每个目标实例来源 |
| 摄像头经纬度 | 地图轨迹展示       |
| 摄像头朝向 | 判断进入／离开方向   |
| 覆盖路段   | 绑定道路空间位置     |
| 时间戳     | 跨镜拼接的基础       |
| 道路拓扑   | 判断摄像头之间是否可达 |
| 路段距离   | 计算合理旅行时间     |
| 车道方向   | 过滤逆向或不合理连接 |

摄像头位置、朝向和道路拓扑是轨迹拼接可靠性的基础。没有这些元数据，跨镜拼接只能依赖外观相似度，可靠性会明显下降。

### 4.2 视频结构化感知层

把原始视频变成结构化目标。主要任务：

```text
目标检测：车辆、行人、非机动车、车牌区域
单摄跟踪：生成单摄 tracklet
属性识别：车辆颜色、车型、行人服饰、背包
车牌 OCR：车牌检测与识别
质量评分：清晰度、完整度、遮挡率、检测置信度
特征提取：ReID 向量、CLIP／Chinese-CLIP 图像向量
```

输出单元为：

```text
目标实例 ＋ 关键帧 ＋ 属性标签 ＋ 向量特征 ＋ 单摄轨迹片段
```

### 4.3 单摄轨迹生成层

每个摄像头内生成局部轨迹（tracklet）。示例：

```text
CAM_001 中某辆黑色轿车：
  08:31:12 进入画面
  08:31:18 离开画面
  方向：由西向东
  车牌：苏E******
  车型：轿车
  颜色：黑色
  关键帧：3 张
```

可选算法：ByteTrack / BoT-SORT / DeepSORT / OC-SORT。

单摄轨迹是摄像头视野内的**真实观测段**，可信度最高。

### 4.4 跨镜轨迹拼接层

核心算法模块：

> **参考快速路全样本时空轨迹拼接思路，将单摄 tracklet 作为轨迹节点，基于时空可达性和目标一致性构建跨镜候选连接图，生成带置信度的跨摄像头观测链。**

#### 轨迹拼接的基本单位

- **节点**：单摄 tracklet
- **边**：两个 tracklet 是否可能属于同一目标

示例：

```text
TRK_CAM001_0008 → TRK_CAM005_0021 → TRK_CAM009_0044
```

### 4.5 跨镜候选边生成方法

候选边生成规则（交通约束缩小候选范围）：

1. 摄像头拓扑可达
2. 时间顺序合理
3. 旅行时间在合理范围内
4. 道路方向一致
5. 目标类别一致
6. 车牌不冲突
7. 颜色、车型、服饰等属性不冲突
8. ReID 外观相似度达到阈值

> 示例：车辆从 CAM_A 到 CAM_B 理论行驶时间为 40～150 s。如果一个目标离开 CAM_A 后 10 s 就在 CAM_B 出现，系统应判定为不合理连接。

### 4.6 跨镜连接评分

综合评分公式：

```text
跨镜连接得分
  ＝ 外观相似度
  ＋ 属性一致性
  ＋ 车牌一致性
  ＋ 时间可达性
  ＋ 空间可达性
  ＋ 方向一致性
  － 路径分叉惩罚
  － 观测缺失惩罚
```

**车辆侧权重**：

```text
车牌一致性 ＞ 时间可达性 ＞ 摄像头拓扑 ＞ ReID 外观 ＞ 颜色车型
```

**行人侧权重**：

```text
时空可达性 ＞ ReID 外观 ＞ 服饰属性 ＞ 背包属性
```

车辆有车牌和道路约束，行人没有强身份信息，不能过度信任外观相似度。

---

## 五、轨迹输出方式：观测链＋推断路径

系统输出四类信息：

### 5.1 观测节点

目标在哪些摄像头中被实际看到：

```text
CAM_001：08:31:12
CAM_005：08:34:26
CAM_009：08:37:42
```

### 5.2 观测段

摄像头视野内真实检测到的运动片段（可视化用实线）：

```text
CAM_001 内，从画面左侧进入，从右侧离开
CAM_005 内，沿东向西通过路口
```

### 5.3 推断段

摄像头之间无连续画面，基于道路拓扑和时间推断可能路径（可视化用虚线）：

```text
CAM_001 → CAM_005：推断连接，置信度 0.82
CAM_005 → CAM_009：推断连接，置信度 0.76
```

### 5.4 候选路径

两个摄像头之间有多条可能道路时，返回多条候选路径：

```text
候选路径 1：置信度 0.72
候选路径 2：置信度 0.41
候选路径 3：置信度 0.25
```

---

## 六、文本检索与候选图片召回

### 6.1 一般目标查询

用户输入"蓝色背包的男人"，系统解析为：

```text
目标类别：行人
性别外观：男性
附属物：背包
背包颜色：蓝色
```

检索流程：

```text
目标类别过滤 → 属性过滤 → 图文向量召回 → 候选图片重排 → 返回 Top-K 候选目标图片
```

### 6.2 车牌查询

用户输入"苏E×××××"，系统优先走精确匹配：

```text
车牌 OCR 精确匹配 → 车辆实例召回 → 绑定单摄 tracklet → 返回候选图片
```

车牌查询比"黑色轿车"等模糊描述可靠得多。

---

## 七、用户确认与轨迹回溯

用户从候选图片中确认目标后，系统获得：

```text
instance_id / tracklet_id / camera_id / timestamp
```

回溯流程：

```text
查询所属单摄 tracklet
  ↓
查询跨镜轨迹候选图
  ↓
向上游和下游扩展候选轨迹链
  ↓
根据连接置信度重排
  ↓
返回高置信观测链和候选路径
```

> 用户确认目标后，系统不再单纯依赖文本描述，而是围绕一个确定目标实例进行轨迹回溯，误检风险显著下降。

---

## 八、最终系统输出

系统最终返回十类信息：

1. 候选目标图片
2. 用户确认后的目标实例
3. 目标经过摄像头序列
4. 每个摄像头的关键帧
5. 每个节点的出现时间
6. 摄像头视野内观测段
7. 摄像头之间推断段
8. 候选路网路径
9. 每段连接置信度
10. 车牌、属性、ReID、旅行时间等拼接证据

---

## 九、分期技术路线

### 第一阶段：车辆优先闭环

```text
车牌查询 / 颜色车型查询 → 候选车辆图片返回 → 用户确认 → 返回车辆经过摄像头序列和候选路径
```

选择车辆优先的原因：

- 车辆有车牌（强身份标识）
- 车辆受道路拓扑约束
- 车辆跨镜拼接更稳定
- 业务价值更明确

### 第二阶段：加入行人和非机动车

扩展到行人和非机动车查询，但仅做：

```text
候选召回 → 短距离观测链 → 用户确认 → 高置信节点回溯
```

不承诺长距离连续轨迹。

### 第三阶段：风险感知增强

在轨迹链基础上做交通风险判断：

- 是否经过事故点
- 是否在事故前后出现
- 是否从上游驶来 / 向下游逃离
- 是否存在异常停留
- 是否存在逆行或异常路径
- 是否与其他风险目标存在时空交汇

系统从"检索系统"升级为"交通风险感知系统"。

---

## 十、项目最终表述

本项目构建面向相城区交通风险感知场景的文本驱动目标检索与跨镜时空回溯系统。系统首先对多摄像头道路视频流进行预结构化处理，通过目标检测、单摄跟踪、属性识别、车牌 OCR、质量评分和向量特征提取，生成目标实例库和单摄轨迹库；随后参考快速路全样本时空轨迹拼接思路，将单摄 tracklet 作为轨迹节点，融合摄像头拓扑、道路方向、合理旅行时间、ReID 外观相似度、属性一致性和车牌一致性构建跨镜候选连接图，生成带置信度的跨摄像头观测链。

用户输入"蓝色背包的男人""黑色轿车"或某车牌号车辆后，系统通过查询解析、结构化属性过滤和图文向量召回返回候选目标图片。用户确认目标后，系统以该目标实例为锚点，在跨镜候选图中回溯其经过的摄像头节点、关键时间、观测片段和候选路网路径。考虑到道路摄像头覆盖不连续，系统将摄像头视野内轨迹作为真实观测段，将摄像头之间路径作为带置信度的推断段，并显式输出关联证据和不确定性，避免将离散视频观测误表述为连续真实轨迹。该系统可用于事故回溯、重点车辆排查、风险目标定位和后续人工复核。

---

## 十一、核心方法创新点

### 11.1 从图片检索到目标时空回溯

普通文搜图只回答"哪些图片像？"，本系统回答"目标在哪些摄像头出现过，什么时间出现，可能怎么移动"。

### 11.2 从连续轨迹还原到离散观测链重建

主动承认道路摄像头覆盖不连续，输出：

```text
观测段 ＋ 推断段 ＋ 候选路径 ＋ 置信度
```

### 11.3 从纯视觉匹配到交通约束拼接

跨镜拼接融合摄像头拓扑、道路方向、合理旅行时间、车牌一致性、属性一致性、外观相似度和路径分叉惩罚，体现交通场景算法思考。

---

## 十二、一句话总结

> **在道路摄像头覆盖不连续条件下，基于多摄像头视频结构化、跨镜 tracklet 拼接和文本检索，实现目标候选图片召回、用户确认、摄像头经过链重建和路网候选路径推断的交通目标时空回溯系统。**
